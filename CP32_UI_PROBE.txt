===== emote count=327 =====
--- emote #1 offset=134034 ---
t forwards}.gz-mega-ring{width:46px;height:46px;border:6px solid #ff4157;border-radius:50%;box-shadow:0 0 40px #ff1235;animation:gzRing 1.15s ease-out forwards}
      .vortex-pair{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.vortex-pair>div{padding:8px;border:1px solid #365170;border-radius:12px;background:#071525}.online-room-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;max-height:46vh;overflow:auto}.online-room-card{padding:6px;border:1px solid #46526d;border-radius:10px;background:#10182a;text-align:center}.online-room-card.selected{outline:2px solid #6aff9e;background:#173225}.online-room-card .char-emoji{font-size:28px;min-height:34px}.emote-bar{display:flex;gap:6px;flex-wrap:wrap;margin:9px 0}.emote-bar button{font-size:24px}.battle-emote{position:fixed;z-index:9999;right:18px;top:74px;padding:10px 15px;border-radius:18px;background:#111c38e8;border:2px solid #87a9ff;font-size:28px;animation:emoteFloat 2.2s ease-out forwards}@keyframes emoteFloat{0%{opacity:0;transform:translateY(18px) scale(.7)}20%{opacity:1;transform:none}75%{opacity:1}100%{opacity:0;transform:translateY(-34px) scale(1.1)}}
      @keyframes jcVine{0%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(0)}25%{opacity:1}100%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(1)}}@keyframes jcWrap{0%{opacity:0;transform:translate(-50%,-50%) scale(1.8) rotate(-30deg)}40%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.55) rotate(25deg)}}@keyframes jcLeaf{0%{opacity:0;transform:translate(-50%,10px) scale(.5)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-55px) scale(1.3)}}@keyframes jcTravel{0%{opacity:1;transform:translate(calc(var(--from-x) - 50%),calc(var(--from-y) - 50%))}100%{opacity:0;transform:translate(calc(var(--to-x) - 50%),calc(var(--to-y) - 50%)) scale(1.8)}}@keyframes jcSummon{0%{opacity:0;transform:translate(-50%,-20%) scale(.2) rotate(-90deg)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(1.6) rotate(150deg)}}@keyframes jcClaw{0%{opacity:0;transform:translate(-50%,-50%) scale(2) rotate(-30d
--- emote #2 offset=134094 ---
olid #ff4157;border-radius:50%;box-shadow:0 0 40px #ff1235;animation:gzRing 1.15s ease-out forwards}
      .vortex-pair{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.vortex-pair>div{padding:8px;border:1px solid #365170;border-radius:12px;background:#071525}.online-room-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;max-height:46vh;overflow:auto}.online-room-card{padding:6px;border:1px solid #46526d;border-radius:10px;background:#10182a;text-align:center}.online-room-card.selected{outline:2px solid #6aff9e;background:#173225}.online-room-card .char-emoji{font-size:28px;min-height:34px}.emote-bar{display:flex;gap:6px;flex-wrap:wrap;margin:9px 0}.emote-bar button{font-size:24px}.battle-emote{position:fixed;z-index:9999;right:18px;top:74px;padding:10px 15px;border-radius:18px;background:#111c38e8;border:2px solid #87a9ff;font-size:28px;animation:emoteFloat 2.2s ease-out forwards}@keyframes emoteFloat{0%{opacity:0;transform:translateY(18px) scale(.7)}20%{opacity:1;transform:none}75%{opacity:1}100%{opacity:0;transform:translateY(-34px) scale(1.1)}}
      @keyframes jcVine{0%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(0)}25%{opacity:1}100%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(1)}}@keyframes jcWrap{0%{opacity:0;transform:translate(-50%,-50%) scale(1.8) rotate(-30deg)}40%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.55) rotate(25deg)}}@keyframes jcLeaf{0%{opacity:0;transform:translate(-50%,10px) scale(.5)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-55px) scale(1.3)}}@keyframes jcTravel{0%{opacity:1;transform:translate(calc(var(--from-x) - 50%),calc(var(--from-y) - 50%))}100%{opacity:0;transform:translate(calc(var(--to-x) - 50%),calc(var(--to-y) - 50%)) scale(1.8)}}@keyframes jcSummon{0%{opacity:0;transform:translate(-50%,-20%) scale(.2) rotate(-90deg)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(1.6) rotate(150deg)}}@keyframes jcClaw{0%{opacity:0;transform:translate(-50%,-50%) scale(2) rotate(-30deg)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-5
--- emote #3 offset=134134 ---
w:0 0 40px #ff1235;animation:gzRing 1.15s ease-out forwards}
      .vortex-pair{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.vortex-pair>div{padding:8px;border:1px solid #365170;border-radius:12px;background:#071525}.online-room-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;max-height:46vh;overflow:auto}.online-room-card{padding:6px;border:1px solid #46526d;border-radius:10px;background:#10182a;text-align:center}.online-room-card.selected{outline:2px solid #6aff9e;background:#173225}.online-room-card .char-emoji{font-size:28px;min-height:34px}.emote-bar{display:flex;gap:6px;flex-wrap:wrap;margin:9px 0}.emote-bar button{font-size:24px}.battle-emote{position:fixed;z-index:9999;right:18px;top:74px;padding:10px 15px;border-radius:18px;background:#111c38e8;border:2px solid #87a9ff;font-size:28px;animation:emoteFloat 2.2s ease-out forwards}@keyframes emoteFloat{0%{opacity:0;transform:translateY(18px) scale(.7)}20%{opacity:1;transform:none}75%{opacity:1}100%{opacity:0;transform:translateY(-34px) scale(1.1)}}
      @keyframes jcVine{0%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(0)}25%{opacity:1}100%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(1)}}@keyframes jcWrap{0%{opacity:0;transform:translate(-50%,-50%) scale(1.8) rotate(-30deg)}40%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.55) rotate(25deg)}}@keyframes jcLeaf{0%{opacity:0;transform:translate(-50%,10px) scale(.5)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-55px) scale(1.3)}}@keyframes jcTravel{0%{opacity:1;transform:translate(calc(var(--from-x) - 50%),calc(var(--from-y) - 50%))}100%{opacity:0;transform:translate(calc(var(--to-x) - 50%),calc(var(--to-y) - 50%)) scale(1.8)}}@keyframes jcSummon{0%{opacity:0;transform:translate(-50%,-20%) scale(.2) rotate(-90deg)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(1.6) rotate(150deg)}}@keyframes jcClaw{0%{opacity:0;transform:translate(-50%,-50%) scale(2) rotate(-30deg)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.7) rotate(-12deg)}}@keyframes
--- emote #4 offset=134296 ---
iv{padding:8px;border:1px solid #365170;border-radius:12px;background:#071525}.online-room-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;max-height:46vh;overflow:auto}.online-room-card{padding:6px;border:1px solid #46526d;border-radius:10px;background:#10182a;text-align:center}.online-room-card.selected{outline:2px solid #6aff9e;background:#173225}.online-room-card .char-emoji{font-size:28px;min-height:34px}.emote-bar{display:flex;gap:6px;flex-wrap:wrap;margin:9px 0}.emote-bar button{font-size:24px}.battle-emote{position:fixed;z-index:9999;right:18px;top:74px;padding:10px 15px;border-radius:18px;background:#111c38e8;border:2px solid #87a9ff;font-size:28px;animation:emoteFloat 2.2s ease-out forwards}@keyframes emoteFloat{0%{opacity:0;transform:translateY(18px) scale(.7)}20%{opacity:1;transform:none}75%{opacity:1}100%{opacity:0;transform:translateY(-34px) scale(1.1)}}
      @keyframes jcVine{0%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(0)}25%{opacity:1}100%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(1)}}@keyframes jcWrap{0%{opacity:0;transform:translate(-50%,-50%) scale(1.8) rotate(-30deg)}40%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.55) rotate(25deg)}}@keyframes jcLeaf{0%{opacity:0;transform:translate(-50%,10px) scale(.5)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-55px) scale(1.3)}}@keyframes jcTravel{0%{opacity:1;transform:translate(calc(var(--from-x) - 50%),calc(var(--from-y) - 50%))}100%{opacity:0;transform:translate(calc(var(--to-x) - 50%),calc(var(--to-y) - 50%)) scale(1.8)}}@keyframes jcSummon{0%{opacity:0;transform:translate(-50%,-20%) scale(.2) rotate(-90deg)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(1.6) rotate(150deg)}}@keyframes jcClaw{0%{opacity:0;transform:translate(-50%,-50%) scale(2) rotate(-30deg)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.7) rotate(-12deg)}}@keyframes jcCoil{0%{opacity:0;transform:translate(-50%,-50%) scale(1.6)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.45)}}@keyframes circuitRing{0%{o
--- emote #5 offset=134341 ---
r-radius:12px;background:#071525}.online-room-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px;max-height:46vh;overflow:auto}.online-room-card{padding:6px;border:1px solid #46526d;border-radius:10px;background:#10182a;text-align:center}.online-room-card.selected{outline:2px solid #6aff9e;background:#173225}.online-room-card .char-emoji{font-size:28px;min-height:34px}.emote-bar{display:flex;gap:6px;flex-wrap:wrap;margin:9px 0}.emote-bar button{font-size:24px}.battle-emote{position:fixed;z-index:9999;right:18px;top:74px;padding:10px 15px;border-radius:18px;background:#111c38e8;border:2px solid #87a9ff;font-size:28px;animation:emoteFloat 2.2s ease-out forwards}@keyframes emoteFloat{0%{opacity:0;transform:translateY(18px) scale(.7)}20%{opacity:1;transform:none}75%{opacity:1}100%{opacity:0;transform:translateY(-34px) scale(1.1)}}
      @keyframes jcVine{0%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(0)}25%{opacity:1}100%{opacity:0;transform:rotate(var(--beam-angle)) scaleX(1)}}@keyframes jcWrap{0%{opacity:0;transform:translate(-50%,-50%) scale(1.8) rotate(-30deg)}40%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.55) rotate(25deg)}}@keyframes jcLeaf{0%{opacity:0;transform:translate(-50%,10px) scale(.5)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-55px) scale(1.3)}}@keyframes jcTravel{0%{opacity:1;transform:translate(calc(var(--from-x) - 50%),calc(var(--from-y) - 50%))}100%{opacity:0;transform:translate(calc(var(--to-x) - 50%),calc(var(--to-y) - 50%)) scale(1.8)}}@keyframes jcSummon{0%{opacity:0;transform:translate(-50%,-20%) scale(.2) rotate(-90deg)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(1.6) rotate(150deg)}}@keyframes jcClaw{0%{opacity:0;transform:translate(-50%,-50%) scale(2) rotate(-30deg)}30%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.7) rotate(-12deg)}}@keyframes jcCoil{0%{opacity:0;transform:translate(-50%,-50%) scale(1.6)}45%{opacity:1}100%{opacity:0;transform:translate(-50%,-50%) scale(.45)}}@keyframes circuitRing{0%{opacity:0;transform:translate(-50%,-50%) scale
--- emote #6 offset=200254 ---
 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-w
--- emote #7 offset=200463 ---
scroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  <
--- emote #8 offset=200631 ---
 text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemF
--- emote #9 offset=273969 ---
), surface = String(anchor.dataset.infoSurface || anchor.dataset.infoCard || ""), team = String(anchor.dataset.infoTeam || "P");
          let deck = null, plusStarChoice = null;
          if (["local-composition","ai-composition","online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effec
--- emote #10 offset=274010 ---
rface || anchor.dataset.infoCard || ""), team = String(anchor.dataset.infoTeam || "P");
          let deck = null, plusStarChoice = null;
          if (["local-composition","ai-composition","online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effectiveUnit, skill),
            description
--- emote #11 offset=274050 ---
 team = String(anchor.dataset.infoTeam || "P");
          let deck = null, plusStarChoice = null;
          if (["local-composition","ai-composition","online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effectiveUnit, skill),
            description:this.skillDescription(effectiveUnit, sk
--- emote #12 offset=274118 ---
 null, plusStarChoice = null;
          if (["local-composition","ai-composition","online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effectiveUnit, skill),
            description:this.skillDescription(effectiveUnit, skill),
          })));
          const deckSlot = effectiveUnit?.deck
--- emote #13 offset=274157 ---
 if (["local-composition","ai-composition","online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effectiveUnit, skill),
            description:this.skillDescription(effectiveUnit, skill),
          })));
          const deckSlot = effectiveUnit?.deckSlotId || effectiveUnit?.compositionSlo
--- emote #14 offset=274201 ---
online-lobby","host-lobby","guest-lobby"].includes(surface)) {
            deck = selected; plusStarChoice = plusStarSelection;
          } else if (["boss-composition","boss-coop","boss-coop-host","boss-coop-guest"].includes(surface)) {
            const bossContext = window.__redTimeoraBossRaid?.presentationContext?.() || null;
            deck = bossContext?.deck || null; plusStarChoice = bossContext?.plusStar || null;
          } else if (surface === "online-remote-lobby") {
            deck = p2p?.remoteDeck || []; plusStarChoice = p2p?.remotePlusStar || null;
          } else if (surface === "boss-coop-remote") {
            deck = p2p?.bossRemoteDeck || []; plusStarChoice = p2p?.bossRemotePlusStar || null;
          }
          if (!Array.isArray(deck)) return Object.freeze({card:def.id,name:def.name,plusStar:false,deckSlotId:null,presentationOnly:true});
          return this.syntheticUnit(def, { deck, plusStarChoice, team, slotIndex:Number(anchor.dataset.infoSlotIndex), surface });
        },
        resolve(cardRef, unit = null, context = {}) {
          const def = unit?.card ? card(unit) : card(cardRef);
          const effectiveUnit = unit || this.syntheticUnit(def, context);
          const plusStar = PlusStarModifierOwner.active(effectiveUnit);
          const rawRarity = String(def.grade || "0☆"), rarityNumber = Number.parseFloat(rawRarity);
          const displayName = `${String(def.name || effectiveUnit?.name || "").replace(/\++$/g, "")}${plusStar ? "+" : ""}`;
          const rarity = plusStar && Number.isFinite(rarityNumber) ? `${rarityNumber + 0.5}☆` : rawRarity;
          const skills = Object.freeze((def.skills || []).map(skill => Object.freeze({
            id:skill.id, name:skill.name,
            cost:PlusStarModifierOwner.cost(effectiveUnit, skill, skill.cost),
            target:PlusStarModifierOwner.target(effectiveUnit, skill),
            description:this.skillDescription(effectiveUnit, skill),
          })));
          const deckSlot = effectiveUnit?.deckSlotId || effectiveUnit?.compositionSlotId || context.deckSlotId || null;
         
--- emote #15 offset=651393 ---
,
        rulesText(mode = leagueSelection, composition = comprehensiveComposition) {
          mode = this.normalizeLeague(mode);
          if (mode === "two") return "2☆ 캐릭터만 4장 선택";
          if (mode === "regular") return "2☆ 2장 + 3☆ 2장";
          if (mode === "comprehensive") return `종합 구성 · ${this.pattern(mode, composition).label}`;
          return "전체 등급 자유 편성";
        },
      });
      let leagueSelection = "mega",
        comprehensiveComposition = "2234",
        irumaForm = "iruma",
        bkenoForm = "bkeno",
        horizonForm = "flameflower",
        plusStarSelection = null,
        onlineSide = "P",
        p2p = { peer: null, conn: null, role: null, connected: false, remoteDeck: null, remotePlusStar: null, localComposition: null, remoteComposition: null, roomCode: "", rules: null },
        shownPvpResults = new WeakSet(),
        shownEndResults = new WeakSet(),
        bgm = {
          audio: null,
          started: false
        };
      const BGM_AUDIO_SRC = "data:audio/mpeg;base64,
--- emote #16 offset=651411 ---
t(mode = leagueSelection, composition = comprehensiveComposition) {
          mode = this.normalizeLeague(mode);
          if (mode === "two") return "2☆ 캐릭터만 4장 선택";
          if (mode === "regular") return "2☆ 2장 + 3☆ 2장";
          if (mode === "comprehensive") return `종합 구성 · ${this.pattern(mode, composition).label}`;
          return "전체 등급 자유 편성";
        },
      });
      let leagueSelection = "mega",
        comprehensiveComposition = "2234",
        irumaForm = "iruma",
        bkenoForm = "bkeno",
        horizonForm = "flameflower",
        plusStarSelection = null,
        onlineSide = "P",
        p2p = { peer: null, conn: null, role: null, connected: false, remoteDeck: null, remotePlusStar: null, localComposition: null, remoteComposition: null, roomCode: "", rules: null },
        shownPvpResults = new WeakSet(),
        shownEndResults = new WeakSet(),
        bgm = {
          audio: null,
          started: false
        };
      const BGM_AUDIO_SRC = "data:audio/mpeg;base64,
--- emote #17 offset=651457 ---
hensiveComposition) {
          mode = this.normalizeLeague(mode);
          if (mode === "two") return "2☆ 캐릭터만 4장 선택";
          if (mode === "regular") return "2☆ 2장 + 3☆ 2장";
          if (mode === "comprehensive") return `종합 구성 · ${this.pattern(mode, composition).label}`;
          return "전체 등급 자유 편성";
        },
      });
      let leagueSelection = "mega",
        comprehensiveComposition = "2234",
        irumaForm = "iruma",
        bkenoForm = "bkeno",
        horizonForm = "flameflower",
        plusStarSelection = null,
        onlineSide = "P",
        p2p = { peer: null, conn: null, role: null, connected: false, remoteDeck: null, remotePlusStar: null, localComposition: null, remoteComposition: null, roomCode: "", rules: null },
        shownPvpResults = new WeakSet(),
        shownEndResults = new WeakSet(),
        bgm = {
          audio: null,
          started: false
        };
      const BGM_AUDIO_SRC = "data:audio/mpeg;base64,
--- emote #18 offset=6039804 ---
CTOR_MISSING");
            if ((!actor || actor.dead) && !replacementRequired) reasons.push("CURRENT_ACTOR_NOT_ALIVE");
            if (!context?.skipActiveIdentityChecks && g.activeTeamId && g.activeTeamId !== currentActorCanonicalTeam) reasons.push("ACTIVE_TEAM_MISMATCH");
            if (!context?.skipActiveIdentityChecks && g.activeEntityId && currentActorUid && g.activeEntityId !== currentActorUid) reasons.push("ACTIVE_ENTITY_MISMATCH");
            if (g.mode === "spectator") reasons.push("SPECTATOR_READ_ONLY");
            if (g.mode === "aivai" || g.mode === "ai-ai") reasons.push("AI_CONTROLLED_MODE");
            if (currentActorCanonicalTeam !== canonicalLocalTeam) reasons.push("REMOTE_ACTOR_TURN");
            if (standardOnline) {
              const expectedTeam = online.role === "host" ? "P" : online.role === "guest" ? "A" : null;
              if (!online.connected) reasons.push("TRANSPORT_NOT_CONNECTED");
              if (!online.battleStarted) reasons.push("BATTLE_START_NOT_COMMITTED");
              if (expectedTeam && canonicalLocalTeam !== expectedTeam) reasons.push("ROLE_CANONICAL_TEAM_MISMATCH");
              if (runtime.generation > 0 && !runtime.committed) reasons.push("SESSION_NOT_COMMITTED");
              if (runtime.generation > 0 && runtime.state !== "IN_BATTLE") reasons.push(`SESSION_${runtime.state || "UNKNOWN"}`);
              if (online.reconnecting) reasons.push("RECONNECTING");
            }
            if (g?.bossRaid?.online) {
              if (!online.connected) reasons.push("TRANSPORT_NOT_CONNECTED");
              if (!online.battleStarted) reasons.push("BATTLE_START_NOT_COMMITTED");
              if (runtime.generation > 0 && (!runtime.committed || runtime.state !== "IN_BATTLE")) reasons.push(`SESSION_${runtime.state || "NOT_COMMITTED"}`);
              if (online.reconnecting) reasons.push("RECONNECTING");
            }
            if (g?.bossRaid?.online && !providers.bossControls?.(g)) reasons.push("BOSS_SLOT_NOT_LOCAL");
            if (locks.length) reasons.push("ACTIVE_INPUT_LOCK");
            if (clock.locked) reaso
--- emote #19 offset=6099432 ---
"button")||[])];
          const actionButtons=buttons.filter(button=>!button.dataset.actionPanelMeta);
          return Object.freeze({
            rootConnected:!!root?.isConnected,
            buttonCount:buttons.length,
            actionButtonCount:actionButtons.length,
            enabledActionButtonCount:actionButtons.filter(button=>!button.disabled).length,
            fingerprint:actionButtons.map(button=>`${button.disabled?"0":"1"}:${button.textContent?.replace(/\s+/g," ").trim()||""}`).join("|"),
          });
        };
        const mustBeEmpty = view => {
          const reasons=view.projection.reasons||[];
          return view.terminal || reasons.some(reason=>[
            "REMOTE_ACTOR_TURN","SPECTATOR_READ_ONLY","AI_CONTROLLED_MODE","PENDING_AUTHORITATIVE_ACTION",
            "PENDING_ONLINE_ACTION","APPLICATION_BUSY","TRANSPORT_NOT_CONNECTED","BATTLE_START_NOT_COMMITTED",
            "RECONNECTING","CLOCK_INPUT_LOCK","ACTIVE_INPUT_LOCK"
          ].includes(reason));
        };
        const matches = (view,renderState=ActionRenderEngine.snapshot(),dom=domState()) => {
          const prior=renderState.lastRender;
          if(!prior||prior.viewModelSignature!==view.signature)return false;
          if(Number(prior.actionPanelViewModelRevision||0)!==Number(view.canonicalRevision||0))return false;
          if(Number(prior.renderedDomRevision||0)!==Number(view.canonicalRevision||0))return false;
          if(prior.actorUid!==(view.projection.currentActorUid||null))return false;
          if(prior.localCanonicalTeam!==(view.projection.canonicalLocalTeam||null))return false;
          if(prior.buttonFingerprint!==dom.fingerprint)return false;
          if(mustBeEmpty(view))return dom.actionButtonCount===0;
          if(view.projection.canAct&&view.projection.manualLegalActionCount>0)return dom.enabledActionButtonCount>0;
          return true;
        };
        const suspend = (reason="action-panel-suspend") => {
          const root=document.getElementById("actionButtons"),buttons=[...(root?.querySelectorAll("button")||[])];
          if(root)root.dat
--- emote #20 offset=6124412 ---
     if (onlineAutoCommandOwner !== automaticOwner) return;
            automaticOwner.timerId = 0;
            if (!game || game.token !== token || game.turnSerial !== serial || game.winner) {
              onlineAutoCommandOwner = null;
              onlineAutoCommandActive = false;
              busy = false;
              renderActions();
              return;
            }
            let executed = false;
            try {
              if (noLegalActions) {
                log(game, `${u?.name || "빈 슬롯"}: 가능한 행동이 없어 턴을 자동으로 넘김.`, "turn");
                endTurn(game, t, s, false);
                executed = true;
              } else executed = !!execute(game, t, s, forcedAction, { remoteAuthoritative:true });
            }
            catch (error) {
              executed = false;
              window.__megaRecordRecoveredError?.(error, "scheduleAI.online-automatic");
              if (window.DEBUG_BATTLE_EVENTS) console.error("[online auto-command]", error);
            }
            if (onlineAutoCommandOwner !== automaticOwner) return;
            onlineAutoCommandOwner = null;
            onlineAutoCommandActive = false;
            busy = false;
            if (!executed) {
              log(game, `${u?.name || "빈 슬롯"}: 예약 행동 검증 실패, 턴을 안전하게 종료.`, "turn");
              endTurn(game, t, s, false);
            }
            afterAction();
          };
          const delay = true ? Math.max(Number($("#speed")?.value || 900), visualHoldMs) : 0;
          automaticOwner.timerId = window.OnlineRuntime?.sessionScope?.setTimeout?.(runAutomatic,delay,"pvp-online-automatic",{phase:"SESSION"}) || window.TimerManager.setTimeout(runAutomatic,delay);
          return;
        }

        if (t === "P" && game.mode === "human" && !forced) return;
        busy = true;
        renderActions();
        scheduleLocalAutoTurnTimer(
          forced ? `scheduled:${forcedAction?.type || "forced"}` : "ai-turn",
          true ? Math.max(Number($("#speed")?.value || 900), visualHoldMs) : 0,
          () => {
            if (!game || game.token !== token || game.turnSerial !== s
--- emote #21 offset=6129329 ---
eLeague(isOnline ? (sharedRules.league || leagueSelection) : leagueSelection),
          fallbackComposition = battleLeague === "comprehensive"
            ? LeagueFormatOwner.normalizeComposition(isOnline ? (p2p?.localComposition || sharedRules.composition || comprehensiveComposition) : comprehensiveComposition)
            : comprehensiveComposition;
        let seed =
            seedOverride || $("#seedInput").value.trim() || String(Date.now()),
          pDeck = onlineConfig?.pDeck || [...selected],
          aDeck = onlineConfig?.aDeck || buildAIDeck(seed, battleLeague, fallbackComposition),
          battlePlusStar = onlineConfig?.plusStar || { P: plusStarSelection, A: onlineConfig?.remotePlusStar || null },
          battleCompositions = LeagueFormatOwner.normalizeTeamCompositions(
            battleLeague,
            onlineConfig?.leagueCompositions || onlineConfig?.compositions || sharedRules?.compositions || {
              P: fallbackComposition,
              A: isOnline ? (p2p?.remoteComposition || null) : fallbackComposition,
            },
            { P:pDeck, A:aDeck },
            battlePlusStar,
          ),
          battleComposition = battleCompositions.P;
        if (
          !LeagueFormatOwner.completeValid(pDeck, battleLeague, battleCompositions.P, battlePlusStar?.P || null, "P") ||
          !LeagueFormatOwner.completeValid(aDeck, battleLeague, battleCompositions.A, battlePlusStar?.A || null, "A")
        )
          return;
        clearLocalAutoTurnTimer("battle-start");
        localBackgroundAutoTurnOwner = null;
        localBattleBackgrounded = false;
        game = newGame(pDeck, aDeck, {
          seed,
          variance: isOnline ? false : $("#variance").checked,
          difficulty: isOnline ? "normal" : $("#difficulty").value,
          mode: onlineConfig?.mode || $("#mode").value,
          league: battleLeague,
          leagueComposition: battleLeague === "comprehensive" ? battleComposition : null,
          leagueCompositions: battleCompositions,
          firstTeam: onlineConfig?.firstTeam || null,
          onlineCoin: 
--- emote #22 offset=6129638 ---
tion)
            : comprehensiveComposition;
        let seed =
            seedOverride || $("#seedInput").value.trim() || String(Date.now()),
          pDeck = onlineConfig?.pDeck || [...selected],
          aDeck = onlineConfig?.aDeck || buildAIDeck(seed, battleLeague, fallbackComposition),
          battlePlusStar = onlineConfig?.plusStar || { P: plusStarSelection, A: onlineConfig?.remotePlusStar || null },
          battleCompositions = LeagueFormatOwner.normalizeTeamCompositions(
            battleLeague,
            onlineConfig?.leagueCompositions || onlineConfig?.compositions || sharedRules?.compositions || {
              P: fallbackComposition,
              A: isOnline ? (p2p?.remoteComposition || null) : fallbackComposition,
            },
            { P:pDeck, A:aDeck },
            battlePlusStar,
          ),
          battleComposition = battleCompositions.P;
        if (
          !LeagueFormatOwner.completeValid(pDeck, battleLeague, battleCompositions.P, battlePlusStar?.P || null, "P") ||
          !LeagueFormatOwner.completeValid(aDeck, battleLeague, battleCompositions.A, battlePlusStar?.A || null, "A")
        )
          return;
        clearLocalAutoTurnTimer("battle-start");
        localBackgroundAutoTurnOwner = null;
        localBattleBackgrounded = false;
        game = newGame(pDeck, aDeck, {
          seed,
          variance: isOnline ? false : $("#variance").checked,
          difficulty: isOnline ? "normal" : $("#difficulty").value,
          mode: onlineConfig?.mode || $("#mode").value,
          league: battleLeague,
          leagueComposition: battleLeague === "comprehensive" ? battleComposition : null,
          leagueCompositions: battleCompositions,
          firstTeam: onlineConfig?.firstTeam || null,
          onlineCoin: onlineConfig?.coinResult || null,
          plusStar: battlePlusStar,
        });
        if (isOnline && game?.mode === "online") {
          p2p.playerId ||= stableOnlinePlayerId();
          p2p.matchId ||= game.matchId || seedOverride || randomId("match");
          game.matchId = p2p.matchId;
          
--- emote #23 offset=6136922 ---
osition || rules?.composition || comprehensiveComposition) : comprehensiveComposition,
          compositionChanged = nextLeague === "comprehensive" && nextComposition !== comprehensiveComposition;
        if (nextLeague === "comprehensive") { comprehensiveComposition = nextComposition; if (p2p) p2p.localComposition = nextComposition; }
        if (nextLeague && (nextLeague !== leagueSelection || compositionChanged)) setLeague(nextLeague, true);
        else syncLeagueSelectionSurface();
      }
      function applySharedOnlineRules(rules) {
        const league = LeagueFormatOwner.normalizeLeague(rules?.league || p2p?.rules?.league || leagueSelection);
        let localComposition = null, remoteComposition = null, publishedComposition = null, teamCompositions = null;
        if (league === "comprehensive") {
          const incoming = rules?.compositions && typeof rules.compositions === "object" ? rules.compositions : null;
          const valid = value => LeagueFormatOwner.comprehensivePatterns[String(value || "").trim()] ? String(value).trim() : null;
          const localTeam = p2p?.role === "guest" ? "A" : "P", remoteTeam = localTeam === "P" ? "A" : "P";
          publishedComposition = valid(rules?.composition) || valid(incoming?.P) || valid(p2p?.rules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localCompositio
--- emote #24 offset=6137357 ---
League, true);
        else syncLeagueSelectionSurface();
      }
      function applySharedOnlineRules(rules) {
        const league = LeagueFormatOwner.normalizeLeague(rules?.league || p2p?.rules?.league || leagueSelection);
        let localComposition = null, remoteComposition = null, publishedComposition = null, teamCompositions = null;
        if (league === "comprehensive") {
          const incoming = rules?.compositions && typeof rules.compositions === "object" ? rules.compositions : null;
          const valid = value => LeagueFormatOwner.comprehensivePatterns[String(value || "").trim()] ? String(value).trim() : null;
          const localTeam = p2p?.role === "guest" ? "A" : "P", remoteTeam = localTeam === "P" ? "A" : "P";
          publishedComposition = valid(rules?.composition) || valid(incoming?.P) || valid(p2p?.rules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection
--- emote #25 offset=6137706 ---
   if (league === "comprehensive") {
          const incoming = rules?.compositions && typeof rules.compositions === "object" ? rules.compositions : null;
          const valid = value => LeagueFormatOwner.comprehensivePatterns[String(value || "").trim()] ? String(value).trim() : null;
          const localTeam = p2p?.role === "guest" ? "A" : "P", remoteTeam = localTeam === "P" ? "A" : "P";
          publishedComposition = valid(rules?.composition) || valid(incoming?.P) || valid(p2p?.rules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection route");
        const list = onlineConnectionRouteRegistry.get(type) || [];
        const existing = list.find(entry => entry.owner === owner);
        if (existing) {
          existing.handler = handler;
          existing.order = Number(order) || 0;
          existing.phases = new Set(phases);
        } else list.push({ owner, handler, order:
--- emote #26 offset=6137743 ---
          const incoming = rules?.compositions && typeof rules.compositions === "object" ? rules.compositions : null;
          const valid = value => LeagueFormatOwner.comprehensivePatterns[String(value || "").trim()] ? String(value).trim() : null;
          const localTeam = p2p?.role === "guest" ? "A" : "P", remoteTeam = localTeam === "P" ? "A" : "P";
          publishedComposition = valid(rules?.composition) || valid(incoming?.P) || valid(p2p?.rules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection route");
        const list = onlineConnectionRouteRegistry.get(type) || [];
        const existing = list.find(entry => entry.owner === owner);
        if (existing) {
          existing.handler = handler;
          existing.order = Number(order) || 0;
          existing.phases = new Set(phases);
        } else list.push({ owner, handler, order: Number(order) || 0, phases: new Set(
--- emote #27 offset=6137818 ---
 === "object" ? rules.compositions : null;
          const valid = value => LeagueFormatOwner.comprehensivePatterns[String(value || "").trim()] ? String(value).trim() : null;
          const localTeam = p2p?.role === "guest" ? "A" : "P", remoteTeam = localTeam === "P" ? "A" : "P";
          publishedComposition = valid(rules?.composition) || valid(incoming?.P) || valid(p2p?.rules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection route");
        const list = onlineConnectionRouteRegistry.get(type) || [];
        const existing = list.find(entry => entry.owner === owner);
        if (existing) {
          existing.handler = handler;
          existing.order = Number(order) || 0;
          existing.phases = new Set(phases);
        } else list.push({ owner, handler, order: Number(order) || 0, phases: new Set(phases) });
        list.sort((a,b)=>a.order-b.order || a.owner.localeCompa
--- emote #28 offset=6138196 ---
ules?.composition) || valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection route");
        const list = onlineConnectionRouteRegistry.get(type) || [];
        const existing = list.find(entry => entry.owner === owner);
        if (existing) {
          existing.handler = handler;
          existing.order = Number(order) || 0;
          existing.phases = new Set(phases);
        } else list.push({ owner, handler, order: Number(order) || 0, phases: new Set(phases) });
        list.sort((a,b)=>a.order-b.order || a.owner.localeCompare(b.owner));
        onlineConnectionRouteRegistry.set(type, list);
        return true;
      }

      // Application packet adapter only. Session identity, generation, binding promotion,
      // reconnect and cleanup are owned exclusively by OnlineRuntime/TransportAdapter.
      const OnlineConnectionEngine = (() => {
        const EVENT_NAMES = Object.freeze(["open","dat
--- emote #29 offset=6138216 ---
| valid(p2p?.localComposition) || comprehensiveComposition;
          localComposition = valid(incoming?.[localTeam]) || valid(p2p?.localComposition) || comprehensiveComposition;
          remoteComposition = valid(incoming?.[remoteTeam]) || (p2p?.role === "guest" ? publishedComposition : valid(p2p?.remoteComposition));
          if (incoming && valid(incoming.P) && valid(incoming.A)) teamCompositions = { P:valid(incoming.P), A:valid(incoming.A) };
        }
        const normalized = onlineRulesPayload(league, publishedComposition || comprehensiveComposition, teamCompositions);
        p2p.rules = normalized;
        p2p.localComposition = localComposition;
        p2p.remoteComposition = remoteComposition;
        if (normalized.league === "comprehensive") comprehensiveComposition = localComposition;
        lockOnlineSettings(true, normalized, localComposition);
        return normalized;
      }
      const onlineConnectionRouteRegistry = new Map();
      function registerOnlineConnectionRoute(type, owner, handler, order = 50, phases = ["session"]) {
        if (typeof type !== "string" || !type || typeof owner !== "string" || !owner || typeof handler !== "function")
          throw new TypeError("invalid online connection route");
        const list = onlineConnectionRouteRegistry.get(type) || [];
        const existing = list.find(entry => entry.owner === owner);
        if (existing) {
          existing.handler = handler;
          existing.order = Number(order) || 0;
          existing.phases = new Set(phases);
        } else list.push({ owner, handler, order: Number(order) || 0, phases: new Set(phases) });
        list.sort((a,b)=>a.order-b.order || a.owner.localeCompare(b.owner));
        onlineConnectionRouteRegistry.set(type, list);
        return true;
      }

      // Application packet adapter only. Session identity, generation, binding promotion,
      // reconnect and cleanup are owned exclusively by OnlineRuntime/TransportAdapter.
      const OnlineConnectionEngine = (() => {
        const EVENT_NAMES = Object.freeze(["open","data","close","error"])
--- emote #30 offset=6168583 ---
sultClose">전장 결과 보기</button><button id="pvpResultMenu">메인 메뉴</button></div>`;
        $("#modal").classList.add("open");
        ResultContributionPresentationOwner.bind($("#modalContent"), game);
        $("#pvpResultClose").onclick = () => $("#modal").classList.remove("open");
        $("#pvpResultMenu").onclick = () => window.OnlineRuntime?.returnToSetup?.("pvp-result-menu");
      }
      const NavigationApplication = (() => {
        const project = (id) => {
          hideCardTooltip();
          $$(".screen").forEach((x) => x.classList.toggle("active", x.id === id));
          if (id !== "game") stopBgm();
          if (id === "setup") {
            document.querySelectorAll(".battle-emote").forEach((node) => node.remove());
            (document.scrollingElement || document.documentElement)?.scrollTo({ top: 0, left: 0, behavior: "auto" });
            syncGrandFieldBackground(null);
          }
          window.__megaSyncOnlineChatUI?.();
          return true;
        };
        const request = (id, reason = "navigation") => {
          if (id === "setup" && game) game.paused = true;
          const committed = project(id);
          return Object.freeze({ committed, id, reason, paused:!!game?.paused });
        };
        const resumeBattle = (reason = "battle-resume", options = {}) => {
          if (game && !game.winner) game.paused = false;
          if (options.render === true && game) render();
          return Object.freeze({ committed:true, reason, paused:!!game?.paused });
        };
        return Object.freeze({ request, resumeBattle });
      })();
      function openChoice(html, items) {
        $("#modalContent").innerHTML = html + '<div class="choice-list"></div>';
        let list = $("#modalContent .choice-list");
        items.forEach((x) => {
          let b = document.createElement("button");
          b.textContent = x.label;
          b.onclick = () => {
            $("#modal").classList.remove("open");
            x.fn();
          };
          list.appendChild(b);
        });
        $("#modal").classList.add("open");
      }
      func
===== emoji count=76 =====
--- emoji #1 offset=23283 ---
ckground: radial-gradient(circle at 50% 0%, #ff647833, transparent 52%);
      }
      .victory-window.defeat .victory-title {
        color: #ff8796;
        text-shadow: 0 0 10px #d9274f, 0 0 28px #7d102faa;
      }
      .victory-window.defeat .victory-sub { color: #ffd3d9; }
      .mvp-card {
        display: grid;
        grid-template-columns: auto minmax(0, 1fr);
        align-items: center;
        gap: 14px;
        max-width: 520px;
        margin: 12px auto 18px;
        padding: 12px 16px;
        border: 1px solid #ffd86b88;
        border-radius: 14px;
        background: #0e1420e8;
        text-align: left;
        box-shadow: 0 0 22px #ffc93d22;
      }
      .mvp-card .char-emoji { width: 92px; min-height: 76px; font-size: 58px; }
      .mvp-card .mvp-label { color: #ffe277; font-size: 13px; font-weight: 900; letter-spacing: .08em; }
      .mvp-card .mvp-name { color: #fff; font-size: 24px; font-weight: 900; margin: 2px 0 5px; }
      .mvp-card .mvp-detail { color: #cbd5e1; font-size: 12px; line-height: 1.55; }
      .victory-stats {
        display: grid;
        grid-template-columns: repeat(3,minmax(0,1fr));
        gap: 8px;
        margin: 16px auto 20px;
        max-width: 680px;
      }
      .victory-stats span {
        padding: 11px 8px; border: 1px solid #ffd86b66; border-radius: 10px;
        background: #17130ccc; color: #fff;
      }
      @keyframes victoryPop { from { opacity:0; transform:scale(.65) translateY(18px); } to { opacity:1; transform:none; } }
      @media (max-width: 620px) { .victory-stats { grid-template-columns: 1fr 1fr; } }
      .choice-list {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
      }
      .summary-table {
        width: 100%;
        border-collapse: collapse;
        font-size: 12px;
      }
      .summary-table th,
      .summary-table td {
        border: 1px solid var(--line);
        padding: 5px;
        text-align: right;
      }
      .summary-table th:first-child,
      .summary-table td:first-child {
        text-align: left;
      }
      .progress {
        height: 12px;
   
--- emoji #2 offset=87681 ---
arent !important;
        box-shadow: 0 0 0 3px var(--warn), 0 0 18px rgba(255,200,87,.35) !important;
        border-radius: 14px;
      }
      .battle-unit-card.targetable {
        box-shadow: 0 0 0 3px var(--ok), 0 0 18px rgba(77,204,136,.32) !important;
        border-radius: 14px;
      }
      .battle-unit-card .unit-stage,
      .battle-unit-card.field-card .unit-stage,
      .battle-unit-card.bench-card .unit-stage {
        min-height: 0 !important;
        padding: 2px 0 0 !important;
        gap: 1px !important;
        background: transparent !important;
        border: 0 !important;
        box-shadow: none !important;
        overflow: visible !important;
      }
      .char-emoji {
        float: right;
        width: 82px;
        height: 104px;
        margin: 0 0 6px 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-heig
--- emoji #3 offset=88192 ---
ap: 1px !important;
        background: transparent !important;
        border: 0 !important;
        box-shadow: none !important;
        overflow: visible !important;
      }
      .char-emoji {
        float: right;
        width: 82px;
        height: 104px;
        margin: 0 0 6px 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-r
--- emoji #4 offset=88199 ---
 !important;
        background: transparent !important;
        border: 0 !important;
        box-shadow: none !important;
        overflow: visible !important;
      }
      .char-emoji {
        float: right;
        width: 82px;
        height: 104px;
        margin: 0 0 6px 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 
--- emoji #5 offset=88401 ---
t: right;
        width: 82px;
        height: 104px;
        margin: 0 0 6px 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        te
--- emoji #6 offset=88408 ---
t;
        width: 82px;
        height: 104px;
        margin: 0 0 6px 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shad
--- emoji #7 offset=88477 ---
x 8px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-
--- emoji #8 offset=88484 ---
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 66px;
        line-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-namepla
--- emoji #9 offset=88606 ---
-height: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
    
--- emoji #10 offset=88613 ---
: 1;
        letter-spacing: -0.16em;
        padding-right: .16em;
        user-select: none;
        -webkit-user-select: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char
--- emoji #11 offset=88734 ---
t: none;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      
--- emoji #12 offset=88741 ---
;
        filter: drop-shadow(0 7px 8px rgba(0,0,0,.36));
        transform: none !important;
        animation: none !important;
      }

      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
     
--- emoji #13 offset=88878 ---


      .char-emoji .emoji-part {
        display: inline-block;
        position: relative;
        flex: 0 0 auto;
        margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
      @media (max-width: 620px) {
        .battle-unit-card .char-emoji {
          min-height: 84px;
          font-size: clamp(52px, 17vw, 8
--- emoji #14 offset=88998 ---
     margin-left: -0.34em;
        filter: drop-shadow(0 5px 6px rgba(0,0,0,.25));
      }
      .char-emoji .emoji-part:first-child {
        margin-left: 0;
      }
      .char-emoji .emoji-part:nth-child(odd) {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
      @media (max-width: 620px) {
        .battle-unit-card .char-emoji {
          min-height: 84px;
          font-size: clamp(52px, 17vw, 82px);
        }
        .battle-unit-card.bench-card .char-emoji {
          min-height: 48px;
          font-size: clam
--- emoji #15 offset=89209 ---
 {
        z-index: 2;
        transform: translateY(-0.02em) rotate(-4deg);
      }
      .char-emoji .emoji-part:nth-child(even) {
        z-index: 1;
        transform: translateY(0.05em) rotate(4deg);
      }
      .char-emoji .emoji-part:nth-child(3) {
        z-index: 3;
        transform: translate(-0.03em,-0.18em) scale(.78) rotate(7deg);
      }
      .char-emoji.small {
        width: 72px;
        height: 88px;
        font-size: 52px;
      }
      .battle-unit-card .char-emoji {
        float: none;
        width: 100%;
        height: auto;
        min-height: 104px;
        margin: 0;
        font-size: clamp(62px, 8vw, 104px);
      }
      .battle-unit-card.bench-card .char-emoji {
        min-height: 58px;
        font-size: clamp(38px, 5vw, 62px);
      }
      .battle-unit-card .unit-nameplate,
      .battle-unit-card.bench-card .unit-nameplate {
        position: static !important;
        width: auto !important;
        max-width: 100%;
        margin: 0 !important;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
      @media (max-width: 620px) {
        .battle-unit-card .char-emoji {
          min-height: 84px;
          font-size: clamp(52px, 17vw, 82px);
        }
        .battle-unit-card.bench-card .char-emoji {
          min-height: 48px;
          font-size: clamp(32px, 10vw, 52px);
        }
      }

      /* 모바일 초압축 전투 UI: 세로/가로 한 화면 우선 */
      @media (max-width: 900px) {
        body { font-size: 12px; }
        .app { padding: 3px !important; }
        #game .battl
--- emoji #16 offset=90207 ---
ant;
        padding: 1px 4px 0 !important;
        border: 0 !important;
        border-radius: 0 !important;
        background: transparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
      @media (max-width: 620px) {
        .battle-unit-card .char-emoji {
          min-height: 84px;
          font-size: clamp(52px, 17vw, 82px);
        }
        .battle-unit-card.bench-card .char-emoji {
          min-height: 48px;
          font-size: clamp(32px, 10vw, 52px);
        }
      }

      /* 모바일 초압축 전투 UI: 세로/가로 한 화면 우선 */
      @media (max-width: 900px) {
        body { font-size: 12px; }
        .app { padding: 3px !important; }
        #game .battle { display: block !important; }
        #game .arena {
          gap: 3px !important;
          padding-bottom: 86px;
        }
        .turnstrip {
          min-height: 27px !important;
          height: 27px;
          padding: 2px !important;
          gap: 3px !important;
          border-radius: 6px !important;
        }
        .turnstrip .turntoken {
          padding: 3px 4px !important;
          font-size: 9px !important;
          line-height: 1 !important;
          border-radius: 5px !important;
        }
        .side {
          padding: 4px !important;
          border-radius: 8px !important;
        }
        .side-head {
          margin-bottom: 2px !important;
          min-height: 18px;
          align-items: center;
          font-size: 11px;
          line-height: 1.05;
        }
        .side-head b { font-size: 12px; }
        .side .zone-label { display: none !important; }
        .slots {
          gap: 3px !important;
          margin: 0 !important;
     
--- emoji #17 offset=90342 ---
sparent !important;
        box-shadow: none !important;
        color: #fff !important;
        font-weight: 900 !important;
        text-align: center;
        text-shadow: 0 2px 5px #000;
      }
      .battle-unit-card.field-card .unit-nameplate { font-size: 15px !important; }
      .battle-unit-card.bench-card .unit-nameplate { font-size: 12px !important; }
      .char-picto,
      .char-picto-img,
      .char-picto-core,
      .char-picto-label {
        display: none !important;
      }
      @media (max-width: 620px) {
        .battle-unit-card .char-emoji {
          min-height: 84px;
          font-size: clamp(52px, 17vw, 82px);
        }
        .battle-unit-card.bench-card .char-emoji {
          min-height: 48px;
          font-size: clamp(32px, 10vw, 52px);
        }
      }

      /* 모바일 초압축 전투 UI: 세로/가로 한 화면 우선 */
      @media (max-width: 900px) {
        body { font-size: 12px; }
        .app { padding: 3px !important; }
        #game .battle { display: block !important; }
        #game .arena {
          gap: 3px !important;
          padding-bottom: 86px;
        }
        .turnstrip {
          min-height: 27px !important;
          height: 27px;
          padding: 2px !important;
          gap: 3px !important;
          border-radius: 6px !important;
        }
        .turnstrip .turntoken {
          padding: 3px 4px !important;
          font-size: 9px !important;
          line-height: 1 !important;
          border-radius: 5px !important;
        }
        .side {
          padding: 4px !important;
          border-radius: 8px !important;
        }
        .side-head {
          margin-bottom: 2px !important;
          min-height: 18px;
          align-items: center;
          font-size: 11px;
          line-height: 1.05;
        }
        .side-head b { font-size: 12px; }
        .side .zone-label { display: none !important; }
        .slots {
          gap: 3px !important;
          margin: 0 !important;
        }
        .side .slot {
          min-height: 0 !important;
          padding: 1px !important;
          border-radius: 7px !importa
--- emoji #18 offset=93145 ---
        }
        .unit-energy-hud .e,
        .side .bench .battle-unit-card.bench-card .unit-energy-hud .e {
          width: 13px !important;
          height: 13px !important;
          min-width: 13px !important;
          flex-basis: 13px !important;
          font-size: 7px !important;
          border-width: 1px !important;
        }
        .unit-energy-hud .energy-owner { font-size: 6px !important; }
        .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
          min-height: 58px !important;
          padding: 0 !important;
          gap: 0 !important;
        }
        .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
          min-height: 41px !important;
          height: 41px !important;
          font-size: clamp(35px, 11vw, 48px) !important;
          line-height: 1 !important;
        }
        .battle-unit-card.field-card .unit-nameplate,
        .side .slot > .battle-unit-card .unit-nameplate {
          font-size: 11px !important;
          line-height: 1 !important;
          padding: 0 2px !important;
        }
        .side .bench {
          display: grid !important;
          grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
          gap: 3px !important;
          width: 100% !important;
          overflow: visible !important;
          padding: 0 !important;
          margin: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card {
          width: auto !important;
          max-width: none !important;
          flex: none !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-stage {
          min-height: 38px !important;
          padding: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card .char-emoji {
          min-height: 25px !important;
          height: 25px !important;
          font-size: clamp(23px, 7vw, 31px) !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-nameplate {
          font-size: 9px !important;

--- emoji #19 offset=93198 ---
.bench .battle-unit-card.bench-card .unit-energy-hud .e {
          width: 13px !important;
          height: 13px !important;
          min-width: 13px !important;
          flex-basis: 13px !important;
          font-size: 7px !important;
          border-width: 1px !important;
        }
        .unit-energy-hud .energy-owner { font-size: 6px !important; }
        .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
          min-height: 58px !important;
          padding: 0 !important;
          gap: 0 !important;
        }
        .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
          min-height: 41px !important;
          height: 41px !important;
          font-size: clamp(35px, 11vw, 48px) !important;
          line-height: 1 !important;
        }
        .battle-unit-card.field-card .unit-nameplate,
        .side .slot > .battle-unit-card .unit-nameplate {
          font-size: 11px !important;
          line-height: 1 !important;
          padding: 0 2px !important;
        }
        .side .bench {
          display: grid !important;
          grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
          gap: 3px !important;
          width: 100% !important;
          overflow: visible !important;
          padding: 0 !important;
          margin: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card {
          width: auto !important;
          max-width: none !important;
          flex: none !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-stage {
          min-height: 38px !important;
          padding: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card .char-emoji {
          min-height: 25px !important;
          height: 25px !important;
          font-size: clamp(23px, 7vw, 31px) !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-nameplate {
          font-size: 9px !important;
          line-height: 1 !important;
          paddin
--- emoji #20 offset=94297 ---
important;
        }
        .side .bench {
          display: grid !important;
          grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
          gap: 3px !important;
          width: 100% !important;
          overflow: visible !important;
          padding: 0 !important;
          margin: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card {
          width: auto !important;
          max-width: none !important;
          flex: none !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-stage {
          min-height: 38px !important;
          padding: 0 !important;
        }
        .side .bench .battle-unit-card.bench-card .char-emoji {
          min-height: 25px !important;
          height: 25px !important;
          font-size: clamp(23px, 7vw, 31px) !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-nameplate {
          font-size: 9px !important;
          line-height: 1 !important;
          padding: 0 1px !important;
        }
        .side .bench .battle-unit-card.bench-card .unit-bar.hp-bar { height: 8px !important; }
        .side .bench .battle-unit-card.bench-card .unit-bar.shield-bar { height: 3px !important; }
        .side .bench .battle-unit-card.bench-card .unit-bar-value { font-size: 6px !important; }
        .side .bench .battle-unit-card.bench-card .unit-energy-hud {
          min-height: 10px !important;
          max-height: 12px !important;
        }
        .side .unknown-bench {
          grid-column: 1 / -1;
          min-height: 30px !important;
          max-height: 30px !important;
          padding: 4px 7px !important;
          font-size: 10px !important;
          border-radius: 6px !important;
        }
        .unit-effect-badges {
          left: 1px !important;
          right: 1px !important;
          top: 1px !important;
          gap: 1px !important;
        }
        .side .bench .unit-effect-badge {
          padding: 1px 3px !important;
          font-size: 6px !important;
          line-height: 1 !important;
        }
        .actions {
 
--- emoji #21 offset=98755 ---
padding: 7px !important;
          border: 1px solid var(--line);
          box-shadow: 0 -6px 20px rgba(0,0,0,.62);
        }
      }

      @media (max-width: 900px) and (orientation: landscape) {
  #game .arena {
    align-items: start;
  }
  .side-head {
    min-height: 15px !important;
    font-size: 10px !important;
  }
  .side-head b {
    font-size: 10px !important;
  }
  .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
    min-height: 47px !important;
  }
  .side .bench .battle-unit-card.bench-card .unit-stage {
    min-height: 29px !important;
  }
  .side .bench .battle-unit-card.bench-card .char-emoji {
    min-height: 18px !important;
    height: 18px !important;
    font-size: clamp(18px, 3.4vw, 25px) !important;
  }
  .battle-unit-card.field-card .unit-nameplate,
        .side .slot > .battle-unit-card .unit-nameplate {
    font-size: 9px !important;
  }
  .side .bench .battle-unit-card.bench-card .unit-nameplate {
    font-size: 7px !important;
  }
  .unit-bar.hp-bar {
    height: 8px !important;
  }
  .unit-bar.shield-bar {
    height: 3px !important;
  }
  .unit-energy-hud {
    min-height: 10px !important;
    max-height: 11px !important;
  }
  .unit-energy-hud .e,
        .side .bench .battle-unit-card.bench-card .unit-energy-hud .e {
    width: 10px !important;
    height: 10px !important;
    min-width: 10px !important;
    flex-basis: 10px !important;
    font-size: 5px !important;
  }
  .actions {
    max-height: 62px !important;
  }
}

      /* 2026-07-21 모바일 레이아웃 재정리
         - 세로: 행동 버튼을 가로 스크롤하지 않고 최대 5행까지 위로 확장
         - 가로: 양 진영을 위아래로 유지하고, 각 진영의 후열 및 행동 목록을 오른쪽에 배치 */
      @media (max-width: 900px) and (orientation: portrait) {
        #game .arena {
          padding-bottom: min(218px, 35dvh) !important;
        }
        .actions {
          max-height: min(205px, 33dvh) !important;
          overflow-x: hidden !important;
          overflow-y: auto !important;
        }
        .action-buttons {
          display: grid !important;
          grid-temp
--- emoji #22 offset=102354 ---
important;
    max-height: none !important;
  }
  .ai-side .slot,
        .player-side .slot {
    min-width: 0 !important;
    min-height: 0 !important;
    height: 100% !important;
    overflow: hidden !important;
  }
  .ai-side .battle-unit-card,
        .player-side .battle-unit-card {
    height: 100% !important;
    min-width: 0 !important;
    min-height: 0 !important;
  }
  .ai-side .battle-unit-card .unit-stage,
        .player-side .battle-unit-card .unit-stage,
        .ai-side .battle-unit-card.field-card .unit-stage,
        .player-side .battle-unit-card.field-card .unit-stage {
    min-height: 0 !important;
    flex: 1 1 auto !important;
  }
  .ai-side .battle-unit-card .char-emoji,
        .player-side .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(30px, 5.4vw, 54px) !important;
  }
  .ai-side .bench .battle-unit-card.bench-card,
        .player-side .bench .battle-unit-card.bench-card {
    width: 100% !important;
    height: 100% !important;
    max-height: none !important;
    padding: 2px !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .unit-stage,
        .player-side .bench .battle-unit-card.bench-card .unit-stage {
    min-height: 0 !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .char-emoji,
        .player-side .bench .battle-unit-card.bench-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    font-size: clamp(18px, 3.2vw, 31px) !important;
  }
  #actions > b {
    margin-bottom: 4px !important;
  }
}

      /* 2026-07-21 가로모드 최종: 문서 스크롤 없이 한 화면에 전장 고정 */
      @media (max-width: 900px) and (orientation: landscape) {
  html,
        body {
    overflow: hidden !important;
    overscroll-behavior: none !important;
  }
  body {
    inset: 0 !important;
  }
  .app {
    padding: 3px 5px !important;
    display: flex !important;
    flex-direction: column !important;
  }
  #game,
        #game.screen.active {
    flex: 1 1 auto !important;
  }
  #game .battle {
    display: block !important;
  }

--- emoji #23 offset=102406 ---
i-side .slot,
        .player-side .slot {
    min-width: 0 !important;
    min-height: 0 !important;
    height: 100% !important;
    overflow: hidden !important;
  }
  .ai-side .battle-unit-card,
        .player-side .battle-unit-card {
    height: 100% !important;
    min-width: 0 !important;
    min-height: 0 !important;
  }
  .ai-side .battle-unit-card .unit-stage,
        .player-side .battle-unit-card .unit-stage,
        .ai-side .battle-unit-card.field-card .unit-stage,
        .player-side .battle-unit-card.field-card .unit-stage {
    min-height: 0 !important;
    flex: 1 1 auto !important;
  }
  .ai-side .battle-unit-card .char-emoji,
        .player-side .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(30px, 5.4vw, 54px) !important;
  }
  .ai-side .bench .battle-unit-card.bench-card,
        .player-side .bench .battle-unit-card.bench-card {
    width: 100% !important;
    height: 100% !important;
    max-height: none !important;
    padding: 2px !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .unit-stage,
        .player-side .bench .battle-unit-card.bench-card .unit-stage {
    min-height: 0 !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .char-emoji,
        .player-side .bench .battle-unit-card.bench-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    font-size: clamp(18px, 3.2vw, 31px) !important;
  }
  #actions > b {
    margin-bottom: 4px !important;
  }
}

      /* 2026-07-21 가로모드 최종: 문서 스크롤 없이 한 화면에 전장 고정 */
      @media (max-width: 900px) and (orientation: landscape) {
  html,
        body {
    overflow: hidden !important;
    overscroll-behavior: none !important;
  }
  body {
    inset: 0 !important;
  }
  .app {
    padding: 3px 5px !important;
    display: flex !important;
    flex-direction: column !important;
  }
  #game,
        #game.screen.active {
    flex: 1 1 auto !important;
  }
  #game .battle {
    display: block !important;
  }
  #turnStrip {
    height: 24px !important;
    min-
--- emoji #24 offset=103008 ---
rtant;
  }
  .ai-side .battle-unit-card .char-emoji,
        .player-side .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(30px, 5.4vw, 54px) !important;
  }
  .ai-side .bench .battle-unit-card.bench-card,
        .player-side .bench .battle-unit-card.bench-card {
    width: 100% !important;
    height: 100% !important;
    max-height: none !important;
    padding: 2px !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .unit-stage,
        .player-side .bench .battle-unit-card.bench-card .unit-stage {
    min-height: 0 !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .char-emoji,
        .player-side .bench .battle-unit-card.bench-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    font-size: clamp(18px, 3.2vw, 31px) !important;
  }
  #actions > b {
    margin-bottom: 4px !important;
  }
}

      /* 2026-07-21 가로모드 최종: 문서 스크롤 없이 한 화면에 전장 고정 */
      @media (max-width: 900px) and (orientation: landscape) {
  html,
        body {
    overflow: hidden !important;
    overscroll-behavior: none !important;
  }
  body {
    inset: 0 !important;
  }
  .app {
    padding: 3px 5px !important;
    display: flex !important;
    flex-direction: column !important;
  }
  #game,
        #game.screen.active {
    flex: 1 1 auto !important;
  }
  #game .battle {
    display: block !important;
  }
  #turnStrip {
    height: 24px !important;
    min-height: 24px !important;
    padding: 2px !important;
    gap: 3px !important;
    overflow: hidden !important;
  }
  #turnStrip .turntoken {
    min-width: 0 !important;
    height: 20px !important;
    padding: 2px 5px !important;
    font-size: 8px !important;
    line-height: 1.1 !important;
  }
  .ai-side,
        .player-side {
    min-width: 0 !important;
    min-height: 0 !important;
    height: 100% !important;
    overflow: hidden !important;
    padding: 3px !important;
    display: grid !important;
    grid-template-rows: 16px minmax(0, 1fr) !important;
    column-gap: 3px !important
--- emoji #25 offset=103078 ---
ide .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(30px, 5.4vw, 54px) !important;
  }
  .ai-side .bench .battle-unit-card.bench-card,
        .player-side .bench .battle-unit-card.bench-card {
    width: 100% !important;
    height: 100% !important;
    max-height: none !important;
    padding: 2px !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .unit-stage,
        .player-side .bench .battle-unit-card.bench-card .unit-stage {
    min-height: 0 !important;
  }
  .ai-side .bench .battle-unit-card.bench-card .char-emoji,
        .player-side .bench .battle-unit-card.bench-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    font-size: clamp(18px, 3.2vw, 31px) !important;
  }
  #actions > b {
    margin-bottom: 4px !important;
  }
}

      /* 2026-07-21 가로모드 최종: 문서 스크롤 없이 한 화면에 전장 고정 */
      @media (max-width: 900px) and (orientation: landscape) {
  html,
        body {
    overflow: hidden !important;
    overscroll-behavior: none !important;
  }
  body {
    inset: 0 !important;
  }
  .app {
    padding: 3px 5px !important;
    display: flex !important;
    flex-direction: column !important;
  }
  #game,
        #game.screen.active {
    flex: 1 1 auto !important;
  }
  #game .battle {
    display: block !important;
  }
  #turnStrip {
    height: 24px !important;
    min-height: 24px !important;
    padding: 2px !important;
    gap: 3px !important;
    overflow: hidden !important;
  }
  #turnStrip .turntoken {
    min-width: 0 !important;
    height: 20px !important;
    padding: 2px 5px !important;
    font-size: 8px !important;
    line-height: 1.1 !important;
  }
  .ai-side,
        .player-side {
    min-width: 0 !important;
    min-height: 0 !important;
    height: 100% !important;
    overflow: hidden !important;
    padding: 3px !important;
    display: grid !important;
    grid-template-rows: 16px minmax(0, 1fr) !important;
    column-gap: 3px !important;
    row-gap: 2px !important;
    border-radius: 7px !important;
  }

--- emoji #26 offset=107835 ---
rtant;
    max-height: 10px !important;
    gap: 1px !important;
    padding: 0 !important;
    overflow: hidden !important;
  }
  .battle-unit-card .unit-energy-hud .e {
    width: 10px !important;
    height: 10px !important;
    min-width: 10px !important;
    flex: 0 0 10px !important;
    font-size: 5px !important;
    border-width: 1px !important;
  }
  .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .battle-unit-card.bench-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    overflow: hidden !important;
  }
  .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(28px, 5.1vw, 52px) !important;
    line-height: 0.9 !important;
    overflow: visible !important;
  }
  .bench .battle-unit-card .char-emoji,
        .bench .battle-unit-card.bench-card .char-emoji {
    font-size: clamp(18px, 3vw, 30px) !important;
  }
  .battle-unit-card .unit-nameplate {
    margin: 0 !important;
    padding: 0 !important;
    font-size: 8px !important;
    line-height: 1 !important;
    min-height: 9px !important;
    flex: 0 0 auto !important;
  }
  .bench .battle-unit-card .unit-nameplate {
    font-size: 7px !important;
    min-height: 8px !important;
  }
  .unit-effect-badges {
    gap: 1px !important;
    max-height: 13px !important;
    overflow: hidden !important;
  }
  .unit-effect-badge {
    padding: 1px 3px !important;
    font-size: 6px !important;
    line-height: 1 !important;
  }
  #actions.actions {
    grid-area: actions !important;
    inset: auto !important;
    min-width: 0 !important;
  }
  #actions .action-buttons {
    display: grid !important;
    grid-template-columns: 1fr !important;
    grid-auto-rows: minmax(25px, auto) !important;
    align-content: start !important;
    gap: 3px !important;
    padding: 0 1px 1px !important;
  }
  #actions .action-buttons button,

--- emoji #27 offset=107888 ---
!important;
    padding: 0 !important;
    overflow: hidden !important;
  }
  .battle-unit-card .unit-energy-hud .e {
    width: 10px !important;
    height: 10px !important;
    min-width: 10px !important;
    flex: 0 0 10px !important;
    font-size: 5px !important;
    border-width: 1px !important;
  }
  .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .battle-unit-card.bench-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    overflow: hidden !important;
  }
  .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(28px, 5.1vw, 52px) !important;
    line-height: 0.9 !important;
    overflow: visible !important;
  }
  .bench .battle-unit-card .char-emoji,
        .bench .battle-unit-card.bench-card .char-emoji {
    font-size: clamp(18px, 3vw, 30px) !important;
  }
  .battle-unit-card .unit-nameplate {
    margin: 0 !important;
    padding: 0 !important;
    font-size: 8px !important;
    line-height: 1 !important;
    min-height: 9px !important;
    flex: 0 0 auto !important;
  }
  .bench .battle-unit-card .unit-nameplate {
    font-size: 7px !important;
    min-height: 8px !important;
  }
  .unit-effect-badges {
    gap: 1px !important;
    max-height: 13px !important;
    overflow: hidden !important;
  }
  .unit-effect-badge {
    padding: 1px 3px !important;
    font-size: 6px !important;
    line-height: 1 !important;
  }
  #actions.actions {
    grid-area: actions !important;
    inset: auto !important;
    min-width: 0 !important;
  }
  #actions .action-buttons {
    display: grid !important;
    grid-template-columns: 1fr !important;
    grid-auto-rows: minmax(25px, auto) !important;
    align-content: start !important;
    gap: 3px !important;
    padding: 0 1px 1px !important;
  }
  #actions .action-buttons button,
        #actions .action-buttons .skillbtn,
        #
--- emoji #28 offset=108142 ---
px !important;
    border-width: 1px !important;
  }
  .battle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .battle-unit-card.bench-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    overflow: hidden !important;
  }
  .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(28px, 5.1vw, 52px) !important;
    line-height: 0.9 !important;
    overflow: visible !important;
  }
  .bench .battle-unit-card .char-emoji,
        .bench .battle-unit-card.bench-card .char-emoji {
    font-size: clamp(18px, 3vw, 30px) !important;
  }
  .battle-unit-card .unit-nameplate {
    margin: 0 !important;
    padding: 0 !important;
    font-size: 8px !important;
    line-height: 1 !important;
    min-height: 9px !important;
    flex: 0 0 auto !important;
  }
  .bench .battle-unit-card .unit-nameplate {
    font-size: 7px !important;
    min-height: 8px !important;
  }
  .unit-effect-badges {
    gap: 1px !important;
    max-height: 13px !important;
    overflow: hidden !important;
  }
  .unit-effect-badge {
    padding: 1px 3px !important;
    font-size: 6px !important;
    line-height: 1 !important;
  }
  #actions.actions {
    grid-area: actions !important;
    inset: auto !important;
    min-width: 0 !important;
  }
  #actions .action-buttons {
    display: grid !important;
    grid-template-columns: 1fr !important;
    grid-auto-rows: minmax(25px, auto) !important;
    align-content: start !important;
    gap: 3px !important;
    padding: 0 1px 1px !important;
  }
  #actions .action-buttons button,
        #actions .action-buttons .skillbtn,
        #actions .skillbtn {
    width: 100% !important;
    min-width: 0 !important;
    min-height: 25px !important;
    max-width: none !important;
    margin: 0 !important;
    padding: 4px 5px !important;
    font-size: 8px !important;
    line-height: 1.08 
--- emoji #29 offset=108199 ---
attle-unit-card .unit-stage,
        .battle-unit-card.field-card .unit-stage,
        .battle-unit-card.bench-card .unit-stage,
        .side .slot > .battle-unit-card .unit-stage {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    overflow: hidden !important;
  }
  .battle-unit-card .char-emoji,
        .side .slot > .battle-unit-card .char-emoji {
    min-height: 0 !important;
    height: auto !important;
    flex: 1 1 auto !important;
    font-size: clamp(28px, 5.1vw, 52px) !important;
    line-height: 0.9 !important;
    overflow: visible !important;
  }
  .bench .battle-unit-card .char-emoji,
        .bench .battle-unit-card.bench-card .char-emoji {
    font-size: clamp(18px, 3vw, 30px) !important;
  }
  .battle-unit-card .unit-nameplate {
    margin: 0 !important;
    padding: 0 !important;
    font-size: 8px !important;
    line-height: 1 !important;
    min-height: 9px !important;
    flex: 0 0 auto !important;
  }
  .bench .battle-unit-card .unit-nameplate {
    font-size: 7px !important;
    min-height: 8px !important;
  }
  .unit-effect-badges {
    gap: 1px !important;
    max-height: 13px !important;
    overflow: hidden !important;
  }
  .unit-effect-badge {
    padding: 1px 3px !important;
    font-size: 6px !important;
    line-height: 1 !important;
  }
  #actions.actions {
    grid-area: actions !important;
    inset: auto !important;
    min-width: 0 !important;
  }
  #actions .action-buttons {
    display: grid !important;
    grid-template-columns: 1fr !important;
    grid-auto-rows: minmax(25px, auto) !important;
    align-content: start !important;
    gap: 3px !important;
    padding: 0 1px 1px !important;
  }
  #actions .action-buttons button,
        #actions .action-buttons .skillbtn,
        #actions .skillbtn {
    width: 100% !important;
    min-width: 0 !important;
    min-height: 25px !important;
    max-width: none !important;
    margin: 0 !important;
    padding: 4px 5px !important;
    font-size: 8px !important;
    line-height: 1.08 !important;
    white-space: normal !important;
    overf
--- emoji #30 offset=130371 ---
8)}}

      /* 13th~14th 4☆ patch */
      .summon-zone{margin:4px 0 8px;padding:7px;border:1px solid rgba(72,170,105,.35);border-radius:12px;background:rgba(5,28,18,.58)}
      .summon-zone-title{font-size:11px;font-weight:900;color:#9be5ad;margin-bottom:5px}.summon-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:6px}
      .summon-card{position:relative;min-height:74px;padding:6px;border-radius:10px;border:1px solid rgba(130,230,150,.35);background:linear-gradient(145deg,rgba(8,45,23,.92),rgba(3,20,14,.94));overflow:hidden}.summon-card.dead{opacity:.42;filter:grayscale(1)}
      .summon-head{display:flex;align-items:center;gap:5px;font-weight:900;font-size:12px}.summon-emoji{font-size:24px}.summon-detail{font-size:10px;line-height:1.35;color:#d6f6df}.summon-ready{color:#ffe483;font-weight:800}.summon-card.targetable{outline:2px solid #70ff9a;box-shadow:0 0 18px #29d261}
      .jc-fx{position:absolute;z-index:80;pointer-events:none;transform:translate(-50%,-50%)}
      .vine-line{height:8px;width:var(--beam-length);left:var(--from-x);top:var(--from-y);transform-origin:0 50%;transform:rotate(var(--beam-angle)) scaleX(0);border-radius:999px;background:linear-gradient(90deg,#173e1c,#37aa54,#0d3518);box-shadow:0 0 9px #40d56a;animation:jcVine .72s ease-out forwards}.vine-wrap{width:74px;height:48px;border:5px solid #36a455;border-radius:50%;box-shadow:0 0 13px #55df76;animation:jcWrap .8s ease-out forwards}.leaf-half{font-weight:1000;color:#efffe9;text-shadow:0 0 10px #8aff9c;animation:jcLeaf 1s ease-out forwards}.steal-spark{width:12px;height:12px;border-radius:50%;background:#fff6a0;box-shadow:0 0 14px #dfff78;animation:jcTravel .75s ease-in forwards}
      .summon-burst{width:86px;height:86px;border-radius:50%;border:5px dashed var(--summon-color,#78d75e);box-shadow:0 0 30px var(--summon-color,#78d75e);animation:jcSummon 1s ease-out forwards}.claw-fx{width:90px;height:74px;border-left:8px solid #6ce278;border-right:8px solid #102b14;transform:translate(-50%,-50%) rotate(-22deg);animation:jcClaw .8s ease-out forwards}.coil-fx{width:100px;height:62px;
===== reaction count=11 =====
--- reaction #1 offset=6078740 ---
gets) ? pendingSkill.targets.length : 0,
          slotTarget: !!pendingSkill?.slotTarget,
          dragonMemory: !!pendingSkill?.actions?.some?.((action) => actionSkillHasTag(action, "copy")),
          phase: game?.phase || null,
        });
        const begin = ({ actions, targets, slotTarget = false, reason = "target-selection" }) => {
          if (!Array.isArray(actions) || !actions.length) return false;
          pendingSkill = { actions, targets: Array.isArray(targets) ? targets : [], slotTarget: !!slotTarget };
          render();
          return Object.freeze({ ok: true, reason, ...snapshot() });
        };
        const clearPending = ({ reason = "target-selection-clear", restoreActionPhase = false, releaseTargetLock = true } = {}) => {
          const g = game;
          const wasTargeting = !!pendingSkill || !!g?.pendingTargetSelection || String(g?.phase || "") === "WAITING_FOR_TARGET";
          pendingSkill = null;
          if (g?.pendingTargetSelection != null) g.pendingTargetSelection = null;
          if (restoreActionPhase && wasTargeting && g && !g.ended && !g.winner) g.phase = "WAITING_FOR_ACTION";
          if (releaseTargetLock && wasTargeting && typeof InputLockManager !== "undefined") InputLockManager.unlock("target-selection");
          return Object.freeze({ ok: true, reason, wasTargeting, phase: g?.phase || null });
        };
        const cancel = (reason = "cancel-target-selection", options = {}) => {
          const dragonMemory = options.dragonMemory === true;
          const result = clearPending({ reason, restoreActionPhase: true, releaseTargetLock: true });
          if (dragonMemory && typeof dmPendingFlow !== "undefined") dmPendingFlow = null;
          if (options.releaseBusy === true && typeof busy !== "undefined") busy = false;
          if (options.closeModal === true) document.querySelector("#modal")?.classList.remove("open");
          if (options.project !== false && game) render();
          return result;
        };
        const choose = (targetRef) => {
          if (!pendingSkill?.actions?.length || !targetRef) return
--- reaction #2 offset=6079088 ---
      if (!Array.isArray(actions) || !actions.length) return false;
          pendingSkill = { actions, targets: Array.isArray(targets) ? targets : [], slotTarget: !!slotTarget };
          render();
          return Object.freeze({ ok: true, reason, ...snapshot() });
        };
        const clearPending = ({ reason = "target-selection-clear", restoreActionPhase = false, releaseTargetLock = true } = {}) => {
          const g = game;
          const wasTargeting = !!pendingSkill || !!g?.pendingTargetSelection || String(g?.phase || "") === "WAITING_FOR_TARGET";
          pendingSkill = null;
          if (g?.pendingTargetSelection != null) g.pendingTargetSelection = null;
          if (restoreActionPhase && wasTargeting && g && !g.ended && !g.winner) g.phase = "WAITING_FOR_ACTION";
          if (releaseTargetLock && wasTargeting && typeof InputLockManager !== "undefined") InputLockManager.unlock("target-selection");
          return Object.freeze({ ok: true, reason, wasTargeting, phase: g?.phase || null });
        };
        const cancel = (reason = "cancel-target-selection", options = {}) => {
          const dragonMemory = options.dragonMemory === true;
          const result = clearPending({ reason, restoreActionPhase: true, releaseTargetLock: true });
          if (dragonMemory && typeof dmPendingFlow !== "undefined") dmPendingFlow = null;
          if (options.releaseBusy === true && typeof busy !== "undefined") busy = false;
          if (options.closeModal === true) document.querySelector("#modal")?.classList.remove("open");
          if (options.project !== false && game) render();
          return result;
        };
        const choose = (targetRef) => {
          if (!pendingSkill?.actions?.length || !targetRef) return false;
          const matches = pendingSkill.actions.filter((action) =>
            action?.target?.team === targetRef.team &&
            action?.target?.slot === targetRef.slot
          );
          if (!matches.length) return false;
          if (matches.some((action) => actionSkillHasTag(action, "copy"))) {
            const live = matches
--- reaction #3 offset=6079616 ---
.phase || "") === "WAITING_FOR_TARGET";
          pendingSkill = null;
          if (g?.pendingTargetSelection != null) g.pendingTargetSelection = null;
          if (restoreActionPhase && wasTargeting && g && !g.ended && !g.winner) g.phase = "WAITING_FOR_ACTION";
          if (releaseTargetLock && wasTargeting && typeof InputLockManager !== "undefined") InputLockManager.unlock("target-selection");
          return Object.freeze({ ok: true, reason, wasTargeting, phase: g?.phase || null });
        };
        const cancel = (reason = "cancel-target-selection", options = {}) => {
          const dragonMemory = options.dragonMemory === true;
          const result = clearPending({ reason, restoreActionPhase: true, releaseTargetLock: true });
          if (dragonMemory && typeof dmPendingFlow !== "undefined") dmPendingFlow = null;
          if (options.releaseBusy === true && typeof busy !== "undefined") busy = false;
          if (options.closeModal === true) document.querySelector("#modal")?.classList.remove("open");
          if (options.project !== false && game) render();
          return result;
        };
        const choose = (targetRef) => {
          if (!pendingSkill?.actions?.length || !targetRef) return false;
          const matches = pendingSkill.actions.filter((action) =>
            action?.target?.team === targetRef.team &&
            action?.target?.slot === targetRef.slot
          );
          if (!matches.length) return false;
          if (matches.some((action) => actionSkillHasTag(action, "copy"))) {
            const live = matches.filter((action) => {
              if (action?.target?.uid != null && targetRef.uid != null && action.target.uid !== targetRef.uid) return false;
              if (action.copyTargetType === "enemySlot") return true;
              const unit = unitAt(game, action.target.team, action.target.slot);
              return !!unit && !unit.dead && unit.uid === action.target.uid;
            });
            if (!live.length) {
              cancel("dragon-memory-target-stale", { dragonMemory: true, releaseBusy: true, closeModal: t
--- reaction #4 offset=6081147 ---
ion, "copy"))) {
            const live = matches.filter((action) => {
              if (action?.target?.uid != null && targetRef.uid != null && action.target.uid !== targetRef.uid) return false;
              if (action.copyTargetType === "enemySlot") return true;
              const unit = unitAt(game, action.target.team, action.target.slot);
              return !!unit && !unit.dead && unit.uid === action.target.uid;
            });
            if (!live.length) {
              cancel("dragon-memory-target-stale", { dragonMemory: true, releaseBusy: true, closeModal: true });
              return false;
            }
            clearPending({ reason: "dragon-memory-target-selected", restoreActionPhase: false, releaseTargetLock: true });
            if (typeof dmPendingFlow !== "undefined" && dmPendingFlow) dmPendingFlow.stage = "payment";
            dispatchPlayerSkill(live);
            return true;
          }
          clearPending({ reason: "target-selected", restoreActionPhase: false, releaseTargetLock: true });
          dispatchPlayerSkill(matches);
          return true;
        };
        return Object.freeze({ begin, choose, cancel, clearPending, snapshot });
      })();

      function chooseOrExecute(candidates, slotTarget = false) {
        let [t, s] = game.order[game.turn];
        if (
          candidates.length === 1 ||
          !candidates.some((x) => x.target?.team !== t)
        ) {
          dispatchPlayerSkill(candidates);
        } else {
          let seenTargets = new Set(),
            targets = candidates
              .map((x) => x.target)
              .filter((x) => {
                if (!x) return false;
                let key = `${x.team}:${x.slot}`;
                if (seenTargets.has(key)) return false;
                seenTargets.add(key);
                return true;
              });
          TargetSelectionApplication.begin({
            actions: candidates,
            targets,
            slotTarget,
            reason: "skill-target-selection",
          });
        }
      }
      function actionPanelSurfaceContext(g) {
  
--- reaction #5 offset=6081434 ---
nit = unitAt(game, action.target.team, action.target.slot);
              return !!unit && !unit.dead && unit.uid === action.target.uid;
            });
            if (!live.length) {
              cancel("dragon-memory-target-stale", { dragonMemory: true, releaseBusy: true, closeModal: true });
              return false;
            }
            clearPending({ reason: "dragon-memory-target-selected", restoreActionPhase: false, releaseTargetLock: true });
            if (typeof dmPendingFlow !== "undefined" && dmPendingFlow) dmPendingFlow.stage = "payment";
            dispatchPlayerSkill(live);
            return true;
          }
          clearPending({ reason: "target-selected", restoreActionPhase: false, releaseTargetLock: true });
          dispatchPlayerSkill(matches);
          return true;
        };
        return Object.freeze({ begin, choose, cancel, clearPending, snapshot });
      })();

      function chooseOrExecute(candidates, slotTarget = false) {
        let [t, s] = game.order[game.turn];
        if (
          candidates.length === 1 ||
          !candidates.some((x) => x.target?.team !== t)
        ) {
          dispatchPlayerSkill(candidates);
        } else {
          let seenTargets = new Set(),
            targets = candidates
              .map((x) => x.target)
              .filter((x) => {
                if (!x) return false;
                let key = `${x.team}:${x.slot}`;
                if (seenTargets.has(key)) return false;
                seenTargets.add(key);
                return true;
              });
          TargetSelectionApplication.begin({
            actions: candidates,
            targets,
            slotTarget,
            reason: "skill-target-selection",
          });
        }
      }
      function actionPanelSurfaceContext(g) {
        const manual = typeof manualSummonSurfaceContext === "function" ? manualSummonSurfaceContext(g) : null;
        if (manual) return manual;
        return Object.freeze({
          kind:g?.bossRaid ? "boss-raid" : "base",
          eligibilityContext:null,
          manualSummon:nu
--- reaction #6 offset=6447121 ---
onst h = u?.timeRecallHistory || [];
        if (!h.length) return { snapshot:{hp:combatInt(u?.hp||0),statuses:timeStatusClone(u?.statuses),skillSeals:timeStatusClone(u?.skillSeals)}, turnsAgo:0 };
        const index = Math.max(0, h.length - 3);
        return { snapshot:h[index], turnsAgo:Math.min(2, h.length - 1) };
      }

      function timeOrderIndexForSlot(g,t,s) {
        return g?.order?.findIndex(x => x?.[0] === t && Number(x?.[1]) === Number(s)) ?? -1;
      }
      function timeOrderSlotForUnit(g,u) {
        const t=timeTeamOf(g,u),s=g?.teams?.[t]?.field?.findIndex(x=>x?.uid===u?.uid)??-1;
        return {t,s,index:timeOrderIndexForSlot(g,t,s)};
      }
      function timeClockReactionDecision(g,timeora,actor,actorTeam=null,actorSlot=null) {
        if(!timeora||timeora.dead||!cardHasCapability(timeora,"timeControl"))return {ok:false,reason:"inactive"};
        if(status(timeora,"traitSeal"))return {ok:false,reason:"traitSeal"};
        const suppressionDomain=timeCurrentSuppressionDomain(g);
        if(suppressionDomain&&suppressionDomain.suppresses(timeora))return {ok:false,reason:"clockSuppressed"};
        const timePos=timeOrderSlotForUnit(g,timeora);
        let at=actorTeam,as=actorSlot;
        if(at==null||as==null){const p=timeOrderSlotForUnit(g,actor);at=p.t;as=p.s;}
        const actorIndex=timeOrderIndexForSlot(g,at,as),tt=timeTeamOf(g,timeora);
        if(timePos.index>=0&&actorIndex>=0&&at&&tt&&at!==tt){
          const previousIndex=(timePos.index-1+g.order.length)%g.order.length;
          if(actorIndex===previousIndex && !(g?.bossRaid && timeora?.bossRed))return {ok:false,reason:"previousEnemyTurn"};
        }
        return {ok:true,reason:"react"};
      }
      function timeShouldClockReact(g,timeora,actor,actorTeam=null,actorSlot=null) {
        return timeClockReactionDecision(g,timeora,actor,actorTeam,actorSlot).ok;
      }
      function timePaidAmountFromAction(g,t,u,a) {
        const recorded = u?._timePaidRecord;
        if (recorded && Number(recorded.amount) > 0) return recorded.amount;
        if (a?.paymentPlan) return Number
--- reaction #7 offset=6448249 ---
ed"};
        const timePos=timeOrderSlotForUnit(g,timeora);
        let at=actorTeam,as=actorSlot;
        if(at==null||as==null){const p=timeOrderSlotForUnit(g,actor);at=p.t;as=p.s;}
        const actorIndex=timeOrderIndexForSlot(g,at,as),tt=timeTeamOf(g,timeora);
        if(timePos.index>=0&&actorIndex>=0&&at&&tt&&at!==tt){
          const previousIndex=(timePos.index-1+g.order.length)%g.order.length;
          if(actorIndex===previousIndex && !(g?.bossRaid && timeora?.bossRed))return {ok:false,reason:"previousEnemyTurn"};
        }
        return {ok:true,reason:"react"};
      }
      function timeShouldClockReact(g,timeora,actor,actorTeam=null,actorSlot=null) {
        return timeClockReactionDecision(g,timeora,actor,actorTeam,actorSlot).ok;
      }
      function timePaidAmountFromAction(g,t,u,a) {
        const recorded = u?._timePaidRecord;
        if (recorded && Number(recorded.amount) > 0) return recorded.amount;
        if (a?.paymentPlan) return Number(a.paymentPlan.R||0)+Number(a.paymentPlan.W||0)+Number(a.paymentPlan.B||0)+Number(a.extraW||0);
        const base = card(u)?.skills?.find(x => x.id === a?.skill);
        const cost = a?.costOverride ?? base?.cost ?? "";
        return transformedSkillReq(g,t,u,cost).tokens.length + Number(a?.extraW||0);
      }

      function timeRotateClocks(g,t,s,actor,paidAmount,sim=false) {
        const boss=window.__redTimeoraBossRaid;
        if(g?.bossRaid&&typeof boss?.rotateClocksR36==="function")return boss.rotateClocksR36(g,t,s,actor,paidAmount,sim);
        const n = Math.ceil(Math.max(0,Number(paidAmount)||0) - 1e-9);
        if (!n) return [];
        const clocks = [];

        /* 모든 시계의 반응 가능 여부를 회전 시작 시점에 한 번만 스냅샷한다.
           앞에서 처리된 시계가 정렬을 풀거나 새로 맞추더라도, 같은 행동에 반응 중인
           뒤쪽 시계의 정지 여부가 중간에 바뀌어서는 안 된다. */
        const reactionSnapshot = timeFieldTimeoras(g).map(owner => ({
          owner,
          decision: timeClockReactionDecision(g,owner,actor,t,s),
          beforeHour: timeNormDeg(owner.timeHourDeg),
          beforeMinute: timeNormDeg(owner.timeMinuteDeg),
        }));

        for (const s
--- reaction #8 offset=6449372 ---
 x.id === a?.skill);
        const cost = a?.costOverride ?? base?.cost ?? "";
        return transformedSkillReq(g,t,u,cost).tokens.length + Number(a?.extraW||0);
      }

      function timeRotateClocks(g,t,s,actor,paidAmount,sim=false) {
        const boss=window.__redTimeoraBossRaid;
        if(g?.bossRaid&&typeof boss?.rotateClocksR36==="function")return boss.rotateClocksR36(g,t,s,actor,paidAmount,sim);
        const n = Math.ceil(Math.max(0,Number(paidAmount)||0) - 1e-9);
        if (!n) return [];
        const clocks = [];

        /* 모든 시계의 반응 가능 여부를 회전 시작 시점에 한 번만 스냅샷한다.
           앞에서 처리된 시계가 정렬을 풀거나 새로 맞추더라도, 같은 행동에 반응 중인
           뒤쪽 시계의 정지 여부가 중간에 바뀌어서는 안 된다. */
        const reactionSnapshot = timeFieldTimeoras(g).map(owner => ({
          owner,
          decision: timeClockReactionDecision(g,owner,actor,t,s),
          beforeHour: timeNormDeg(owner.timeHourDeg),
          beforeMinute: timeNormDeg(owner.timeMinuteDeg),
        }));

        for (const snap of reactionSnapshot) {
          const {owner,decision,beforeHour,beforeMinute}=snap;
          if (!decision.ok) {
            if(decision.reason==="previousEnemyTurn"){
              if(!sim&&typeof game!=="undefined"&&g===game)
                log(g,`${owner.name}의 시계: 행동순서상 바로 이전 적 차례의 기술에는 반응하지 않음.`,"turn");
              clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour, beforeMinute, afterHour:beforeHour, afterMinute:beforeMinute, hourDelta:0, minuteDelta:0, aligned:timeIsAligned(owner), blocked:true, blockReason:decision.reason });
            }
            /* 특성봉인·상대 정렬로 완전히 정지한 시계는 회전 연출 목록에도 넣지 않는다. */
            continue;
          }
          const hourDelta = 30*n, minuteDelta = owner.uid === actor.uid ? 90*n : 0;
          owner.timeHourDeg = timeNormDeg(beforeHour + hourDelta);
          owner.timeMinuteDeg = timeNormDeg(beforeMinute + minuteDelta);

          clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour,beforeMinute, afterHour:owner.timeHourDeg, afterMinute:owner.timeMinuteDeg, hourDelta,minuteDelta, aligned:timeIsAligned(owner) });
          log(g
--- reaction #9 offset=6449474 ---
medSkillReq(g,t,u,cost).tokens.length + Number(a?.extraW||0);
      }

      function timeRotateClocks(g,t,s,actor,paidAmount,sim=false) {
        const boss=window.__redTimeoraBossRaid;
        if(g?.bossRaid&&typeof boss?.rotateClocksR36==="function")return boss.rotateClocksR36(g,t,s,actor,paidAmount,sim);
        const n = Math.ceil(Math.max(0,Number(paidAmount)||0) - 1e-9);
        if (!n) return [];
        const clocks = [];

        /* 모든 시계의 반응 가능 여부를 회전 시작 시점에 한 번만 스냅샷한다.
           앞에서 처리된 시계가 정렬을 풀거나 새로 맞추더라도, 같은 행동에 반응 중인
           뒤쪽 시계의 정지 여부가 중간에 바뀌어서는 안 된다. */
        const reactionSnapshot = timeFieldTimeoras(g).map(owner => ({
          owner,
          decision: timeClockReactionDecision(g,owner,actor,t,s),
          beforeHour: timeNormDeg(owner.timeHourDeg),
          beforeMinute: timeNormDeg(owner.timeMinuteDeg),
        }));

        for (const snap of reactionSnapshot) {
          const {owner,decision,beforeHour,beforeMinute}=snap;
          if (!decision.ok) {
            if(decision.reason==="previousEnemyTurn"){
              if(!sim&&typeof game!=="undefined"&&g===game)
                log(g,`${owner.name}의 시계: 행동순서상 바로 이전 적 차례의 기술에는 반응하지 않음.`,"turn");
              clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour, beforeMinute, afterHour:beforeHour, afterMinute:beforeMinute, hourDelta:0, minuteDelta:0, aligned:timeIsAligned(owner), blocked:true, blockReason:decision.reason });
            }
            /* 특성봉인·상대 정렬로 완전히 정지한 시계는 회전 연출 목록에도 넣지 않는다. */
            continue;
          }
          const hourDelta = 30*n, minuteDelta = owner.uid === actor.uid ? 90*n : 0;
          owner.timeHourDeg = timeNormDeg(beforeHour + hourDelta);
          owner.timeMinuteDeg = timeNormDeg(beforeMinute + minuteDelta);

          clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour,beforeMinute, afterHour:owner.timeHourDeg, afterMinute:owner.timeMinuteDeg, hourDelta,minuteDelta, aligned:timeIsAligned(owner) });
          log(g, `${owner.name}의 운명의 시계: ${n}E 반응 · 시침 +${hourDelta}°${minuteDelta?` · 분침 +${minuteDelta}°`:""} → ${o
--- reaction #10 offset=6449664 ---
     if(g?.bossRaid&&typeof boss?.rotateClocksR36==="function")return boss.rotateClocksR36(g,t,s,actor,paidAmount,sim);
        const n = Math.ceil(Math.max(0,Number(paidAmount)||0) - 1e-9);
        if (!n) return [];
        const clocks = [];

        /* 모든 시계의 반응 가능 여부를 회전 시작 시점에 한 번만 스냅샷한다.
           앞에서 처리된 시계가 정렬을 풀거나 새로 맞추더라도, 같은 행동에 반응 중인
           뒤쪽 시계의 정지 여부가 중간에 바뀌어서는 안 된다. */
        const reactionSnapshot = timeFieldTimeoras(g).map(owner => ({
          owner,
          decision: timeClockReactionDecision(g,owner,actor,t,s),
          beforeHour: timeNormDeg(owner.timeHourDeg),
          beforeMinute: timeNormDeg(owner.timeMinuteDeg),
        }));

        for (const snap of reactionSnapshot) {
          const {owner,decision,beforeHour,beforeMinute}=snap;
          if (!decision.ok) {
            if(decision.reason==="previousEnemyTurn"){
              if(!sim&&typeof game!=="undefined"&&g===game)
                log(g,`${owner.name}의 시계: 행동순서상 바로 이전 적 차례의 기술에는 반응하지 않음.`,"turn");
              clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour, beforeMinute, afterHour:beforeHour, afterMinute:beforeMinute, hourDelta:0, minuteDelta:0, aligned:timeIsAligned(owner), blocked:true, blockReason:decision.reason });
            }
            /* 특성봉인·상대 정렬로 완전히 정지한 시계는 회전 연출 목록에도 넣지 않는다. */
            continue;
          }
          const hourDelta = 30*n, minuteDelta = owner.uid === actor.uid ? 90*n : 0;
          owner.timeHourDeg = timeNormDeg(beforeHour + hourDelta);
          owner.timeMinuteDeg = timeNormDeg(beforeMinute + minuteDelta);

          clocks.push({ uid:owner.uid, team:timeTeamOf(g,owner), beforeHour,beforeMinute, afterHour:owner.timeHourDeg, afterMinute:owner.timeMinuteDeg, hourDelta,minuteDelta, aligned:timeIsAligned(owner) });
          log(g, `${owner.name}의 운명의 시계: ${n}E 반응 · 시침 +${hourDelta}°${minuteDelta?` · 분침 +${minuteDelta}°`:""} → ${owner.timeHourDeg}°/${owner.timeMinuteDeg}°${timeIsAligned(owner)?" · 정렬":""}.`,"turn");
        }
        if (!sim && typeof game !== "undefined" && g === game && clocks.length) {
          
--- reaction #11 offset=6734537 ---
ossTimeora(g, u, "A"));
    const supportBlocksShared = domainAtStart?.team === "P" &&
      fieldBossOwners.some(u => domainAtStart?.suppresses?.(u));
    const activeSharedOwners = supportBlocksShared ? [] : fieldBossOwners.filter(u =>
      !Number(u?.statuses?.traitSeal || 0) && !domainAtStart?.suppresses?.(u)
    );
    const shared = raidSharedClock(g);
    const sharedBeforeHour = timeNormDeg(shared.hourDeg);
    const sharedBeforeMinute = timeNormDeg(shared.minuteDeg);
    const sharedCanReact = activeSharedOwners.length > 0;

    /* 지원 타임오러의 개인 시계도 행동 시작 시점 기준으로 별도 스냅샷한다. */
    const supportSnapshots = timeFieldTimeoras(g, "P").map(owner => ({
      owner,
      decision: timeClockReactionDecision(g, owner, actor, t, s),
      beforeHour: timeNormDeg(owner.timeHourDeg),
      beforeMinute: timeNormDeg(owner.timeMinuteDeg),
    }));

    if (sharedCanReact) {
      const hourDelta = 30 * n;
      const actorIsBoss = isRaidBossTimeora(g, actor, t);
      const minuteDelta = actorIsBoss ? 90 * n : 0;
      shared.hourDeg = timeNormDeg(sharedBeforeHour + hourDelta);
      shared.minuteDeg = timeNormDeg(sharedBeforeMinute + minuteDelta);
      syncRaidBossClock(g);
      const aligned = raidSharedAligned(g);
      clocks.push({
        uid:"boss-shared-clock", team:"A", bossShared:true,
        beforeHour:sharedBeforeHour, beforeMinute:sharedBeforeMinute,
        afterHour:shared.hourDeg, afterMinute:shared.minuteDeg,
        hourDelta, minuteDelta, aligned
      });
      log(g, `붉은 공용 시계: ${n}E 반응·시침 +${hourDelta}°${minuteDelta ? `·분침 +${minuteDelta}°` : ""}→${shared.hourDeg}°/${shared.minuteDeg}°${aligned ? "·정렬" : ""}.`, "turn");
    }

    for (const snap of supportSnapshots) {
      const {owner, decision, beforeHour, beforeMinute} = snap;
      if (!decision.ok) {
        if (decision.reason === "previousEnemyTurn") {
          if (!sim && typeof game !== "undefined" && g === game)
            log(g, `${owner.name}의 시계: 행동순서상 바로 이전 적 차례의 기술에는 반응하지 않음.`, "turn");
          clocks.push({
            uid:owner.uid, team:"P", beforeHour, beforeMinute,
            
===== 감정 count=1 =====
--- 감정 #1 offset=6359583 ---
   
          const title = p2p.sessionMode === "matchmaking" ? "자동 매칭 대기실" : `온라인 대전 룸 · ${p2p.roomCode}`;
          const currentInvader = currentSelectedInvader(), portalAllowed = LeagueFormatOwner.invaderAllowed(league, composition);
          root.innerHTML = `<h2>${title}</h2><p class="tiny muted">${LeagueFormatOwner.definition(league).label} · ${LeagueFormatOwner.rulesText(league, composition)}</p><p id="roomReadyState">${onlineRoomReadyText()}</p><div id="onlineRoomDeck" class="online-room-grid"></div>${portalAllowed ? InvaderPortalView.buttonHTML({buttonId:"onlineInvaderPortalButton", selectedId:currentInvader, areaClass:"online-invader-portal-area"}) : ""}<div class="emote-bar"><b>감정 표현</b>${["👍","🔥","😮","😂","💢","GG"].map(e => `<button data-emote="${e}">${e}</button>`).join("")}</div><div class="choice-list"><button id="onlineRoomStart" class="primary">${p2p.role === "host" ? "전투 시작" : "상대 준비 대기"}</button><button id="onlineRoomLeave">연결 끊기</button></div><p class="tiny muted">시작 화면과 동일한 리그 구성 규칙을 사용합니다. 폼 캐릭터는 이 리그에서 합법인 폼만 전환됩니다.${portalAllowed ? " 침략자는 초차원 포탈에서 최대 한 명 선택할 수 있습니다." : ""}</p>`;
          const box = document.getElementById("onlineRoomDeck");
          const draw = () => {
            box.replaceChildren();
            const normalPool = LeagueFormatOwner.setupPool(league, composition, selected, plusStarSelection, onlineLocalTeam());
            const seen = new Set();
            for (const rawId of normalPool) {
              const id = rawId;
              if (seen.has(id)) continue;
              seen.add(id);
              const c = CARDS.find(card => card.id === id);
              if (!c || isInvaderCardId(id)) continue;
              const element = document.createElement("div");
              element.tabIndex = 0;
              element.setAttribute("role", "button");
              const slotBlocked = !selected.includes(id) && !LeagueFormatOwner.partialValid([...selected,id], league, composition, plusStarSelection, onlineLocalTeam());
              element.className = `card compact-card selectable online-room-card ${selected.includes(
===== 채팅 count=7 =====
--- 채팅 #1 offset=6506269 ---
nction chooseAI(g, t, s, quick = false) {
        const boss = window.__redTimeoraBossRaid;
        const fallback = () => chooseAISpecialist(g, t, s, quick);
        if (g?.bossRaid && typeof boss?.chooseAIController === "function")
          return boss.chooseAIController(g, t, s, quick, fallback);
        if (g?.bossRaid) return boss?.searchChooseR32?.(g,t,s,quick) || boss?.basicChoose?.(g,t,s,quick) || fallback();
        return fallback();
      }

      /* 타임오러는 초기 덱 목록 렌더 이후 등록되므로 현재 선택창을 한 번 갱신한다. */
      if (document.querySelector('#setup')?.classList.contains('active')) renderSetup(false);

      /* 선택형 코스트(RorB/WorB 등)는 문자 선택이 아니라 실제 에너지 토큰으로 결제한다. */

      /* ===== 온라인 탭 복귀 복구·채팅·동전 면 구분·폭시어스 리워크 ===== */
      (function installOnlineResilienceChatFoxiousPatch(){
        // 모바일 WebView에서 뒷면이 앞면과 같은 색으로 보이는 현상을 결과 확정 시 강제로 교정한다.
        function settleOnlineCoin(coin) {
          if (!coin || coin.classList.contains("settled")) return;
          coin.classList.add("settled");
        }
        document.addEventListener("animationend", event => {
          const coin = event.target?.closest?.(".online-coin");
          if (!coin || event.animationName !== "onlineCoinFlip") return;
          settleOnlineCoin(coin);
        }, true);
        const onlineCoinObserver = new MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(p
--- 채팅 #2 offset=6509597 ---
le retries are immutable: state/turn must stay paired with packet.revision.
          p2p.__reliableStateSentAt = Date.now();
          sendOnlinePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanel
--- 채팅 #3 offset=6509638 ---
 stay paired with packet.revision.
          p2p.__reliableStateSentAt = Date.now();
          sendOnlinePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
    
--- 채팅 #4 offset=6510354 ---
 document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault();
              const input = panel.querySelector("#onlineChatInput");
              const value = String(input.value || "").trim();
              if (!value) return;
              input.value = "";
              sendOnlineChat(value);
            });
          }
          return {button, panel};
        }
        function renderOnlineChatMessages() {
          const box = document.getE
--- 채팅 #5 offset=6513262 ---
tContent = `${message.sender || (message.mine ? "나" : (game?.bossRaid?.online ? "레이드 동료" : "상대"))} · ${chatTimeLabel(message.sentAt)}`;
            row.append(body, meta);
            box.appendChild(row);
          }
          box.scrollTop = box.scrollHeight;
        }
        function syncOnlineChatUI() {
          const {button, panel} = ensureOnlineChatUI();
          const battleScreenOpen = !!document.getElementById("game")?.classList.contains("active");
          const modalOpen = !!document.getElementById("modal")?.classList.contains("open");
          const directOnlineSession = !!(p2p?.connected && (battleScreenOpen || modalOpen));
          // 매칭/레이드 대기, 전투 중 일시 단절, 탭 복귀 재연결 중에도 채팅 UI를 숨기지 않는다.
          const persistentOnlineContext = !!((battleScreenOpen || modalOpen) && (
            p2p?.sessionMode === "matchmaking" ||
            p2p?.sessionMode === "bossRaid" ||
            game?.mode === "online" ||
            game?.mode === "spectator" ||
            !!game?.bossRaid?.online
          ));
          const spectatorSession = !!(game?.mode === "spectator" && window.__megaSpectatorR18?.isWatching?.());
          const spectatorHostSession = !!window.__megaSpectatorR18?.canHostChat?.();
          const connectedSession = directOnlineSession || persistentOnlineContext || spectatorSession || spectatorHostSession;
          if (!connectedSession) onlineChatPanelOpen = false;
          document.body.classList.toggle("online-battle-chat-active", connectedSession);
          button.hidden = !connectedSession;
          button.style.display = connectedSession ? "grid" : "none";
          panel.hidden = !connectedSession || !onlineChatPanelOpen;
          if (!connectedSession) panel.style.display = "none";
          else panel.style.removeProperty("display");
          button.querySelector(".online-chat-unread")?.remove();
          if (onlineChatUnread > 0 && !onlineChatPanelOpen && connectedSession) {
            const badge = document.createElement("span");
            badge.className = "online-chat-unread";
            badge.textContent = onlineChatUn
--- 채팅 #6 offset=6564866 ---
iTitle.textContent = spectatorState.viewSide === "P" ? "붉은 시간 군단" : "레이드 연합";
        } else {
          if (playerTitle) playerTitle.textContent = spectatorState.viewSide === "P" ? "플레이어 1 시점" : "플레이어 2 시점";
          if (aiTitle) aiTitle.textContent = spectatorState.viewSide === "P" ? "플레이어 2" : "플레이어 1";
        }
        const viewButton=bar.querySelector("#spectatorViewButton");if(viewButton)viewButton.textContent=specViewButtonLabel();
        const title = document.getElementById("actionTitle"); if (title) title.textContent = game?.winner ? "경기 종료" : "관전 중";
        const hint = document.getElementById("actionHint"); if (hint) hint.textContent = "읽기 전용 관전 · 상단 시점 버튼으로 양 진영을 전환할 수 있으며 채팅은 사용할 수 있습니다.";
        /* action-panel visibility is committed only by ActionPanelConvergenceOwner. */
      }
      function specApplySnapshot(data) {
        const first = !document.body.classList.contains("spectator-mode") || game?.mode !== "spectator";
        try {
          const revived = data.bossRaid && typeof window.__redTimeoraBossRaid?.revive === "function"
            ? window.__redTimeoraBossRaid.revive(data.state)
            : window.OnlineSnapshotEngine.restoreState(data.state,{source:"spectator",rules:data.rules || ONLINE_COMMON_RULES});
          revived.mode = "spectator";
          revived.spectatorBossRaid = !!data.bossRaid;
          revived.matchId = String(data.matchId || "");
          game = revived;
          onlineSide = spectatorState.viewSide === "A" ? "A" : "P";
          busy = false;
          pendingSkill = null;
          if (first) {
            lastLog = 0;
            visualEvents = [];
            document.getElementById("log").innerHTML = "";
          }
          if (data.visualPacketId && data.visualPacketId !== spectatorState.lastVisualPacketId) {
            spectatorState.lastVisualPacketId = data.visualPacketId;
            visualEvents = cloneOnlineVisualEvents(data.visuals || []);
          } else visualEvents = [];
          document.getElementById("modal").classList.remove("open");
          NavigationApplication.request("game
--- 채팅 #7 offset=6689377 ---
;
    gap:8px;
    padding:4px 2px 10px;
    overscroll-behavior:contain;
  }
  #onlineRoomDeck .online-room-card { min-height:118px; }
  .boss-deck-grid {
    min-height:126px;
    overscroll-behavior:contain;
  }
  .boss-deck-grid:empty::before {
    content:"카드 목록을 불러오는 중…";
    display:grid;place-items:center;min-height:126px;color:#d9bcc1;
  }
  @media(max-width:650px) {
    #modal .modalbox:has(#onlineRoomDeck) {
      width:calc(100vw - 6px);
      max-width:none;
      padding:8px;
    }
    #onlineRoomDeck.online-room-grid {
      grid-template-columns:repeat(2,minmax(0,1fr));
      max-height:55dvh;
    }
    #onlineRoomDeck .online-room-card { min-height:108px; }
  }

  /* 공용 온라인 채팅 헤더와 수동 동기화 버튼. R20 후첨 스타일에서 이전됨. */
/* 기술 버튼의 크기와 배치는 유지하고, 내부 텍스트만 확실히 정중앙에 고정한다. */
  #actions .skillbtn,
  #actionButtons .skillbtn {
    display: flex !important;
    flex-direction: column !important;
    align-items: center !important;
    justify-content: center !important;
    text-align: center !important;
  }
  #actions .skillbtn .skill-heading,
  #actionButtons .skillbtn .skill-heading {
    display: flex !important;
    width: 100% !important;
    align-items: baseline !important;
    justify-content: center !important;
    text-align: center !important;
  }
  #actions .skillbtn > b,
  #actions .skillbtn > .cost,
  #actions .skillbtn > .tiny,
  #actionButtons .skillbtn > b,
  #actionButtons .skillbtn > .cost,
  #actionButtons .skillbtn > .tiny {
    align-self: center !important;
    width: auto !important;
    max-width: 100% !important;
    text-align: center !important;
  }
  #actions .skillbtn > .tiny,
  #actionButtons .skillbtn > .tiny {
    display: block !important;
    width: 100% !important;
  }
  #actions .skillbtn > br,
  #actionButtons .skillbtn > br { display: none !important; }

  .online-chat-head-actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 6px;
    margin-left: auto;
  }
  #onlineBattleSyncBtn {
    position: relative;
    min-width: 66px;
    padding: 6px 9px;
    border-color: rgba(113, 168, 255, .
===== chat count=327 =====
--- chat #1 offset=196354 ---
 cubic-bezier(.16,.72,.18,1) forwards; }
    .time-recall-fx::before,.time-recall-fx::after { content:"";position:absolute;inset:18%;border-radius:50%;border:3px solid var(--time-color);border-left-color:transparent; }
    .time-recall-fx::after { inset:34%;animation-direction:reverse; }
    @keyframes timeRecallSpin { 0%{opacity:0;transform:translate(-50%,-50%) scale(.25) rotate(520deg)} 26%{opacity:1} 100%{opacity:0;transform:translate(-50%,-50%) scale(1.4) rotate(-80deg)} }

    @media (max-width:600px) {
      .time-clock-stack { width:min(82vw,350px); }
      .time-hyper-beam { height:20px; }
      .time-hyper-clock { width:min(86vw,420px); }
    }
  </style>
<style id="onlineResilienceChatFoxiousPatchStyle">
    /* Android 계열 브라우저의 3D 뒷면 렌더링 오차를 막고 결과 면을 확실히 구분한다. */
    .online-coin-gold {
      background:
        radial-gradient(circle at 32% 24%, #fffbd0 0 8%, #ffe15a 19%, #e5a700 58%, #744000 100%) !important;
      border-color: #fff0a0 !important;
      color: #5a3200 !important;
      text-shadow: 0 2px #fff4b6, 0 0 12px rgba(255,209,55,.9) !important;
    }
    .online-coin-mono {
      background:
        radial-gradient(circle at 34% 28%, rgba(255,255,255,.98) 0 11%, transparent 12%),
        conic-gradient(from 45deg, #fafafa 0 25%, #080808 25% 50%, #fafafa 50% 75%, #080808 75% 100%) !important;
      border-color: #151515 !important;
      color: #fff !important;
      text-shadow: 0 2px 5px #000, 0 0 5px #000 !important;
      box-shadow: inset 0 0 0 7px rgba(255,255,255,.16), inset 0 0 28px rgba(0,0,0,.8) !important;
    }
    .online-coin.settled {
      animation: none !important;
      transform: translateY(0) rotateY(var(--coin-end)) rotateX(0) scale(1) !important;
      transform-style: preserve-3d !important;
    }
    .online-coin.settled .online-coin-face {
      display: flex !important;
      backface-visibility: hidden !important;
      -webkit-backface-visibility: hidden !important;
    }
    .online-coin.settled .online-coin-gold { transform: rotateY(0deg) !important; }
    .online-coin.settled .online-coin-mono { transform: rotateY(1
--- chat #2 offset=197791 ---
0, 0 0 5px #000 !important;
      box-shadow: inset 0 0 0 7px rgba(255,255,255,.16), inset 0 0 28px rgba(0,0,0,.8) !important;
    }
    .online-coin.settled {
      animation: none !important;
      transform: translateY(0) rotateY(var(--coin-end)) rotateX(0) scale(1) !important;
      transform-style: preserve-3d !important;
    }
    .online-coin.settled .online-coin-face {
      display: flex !important;
      backface-visibility: hidden !important;
      -webkit-backface-visibility: hidden !important;
    }
    .online-coin.settled .online-coin-gold { transform: rotateY(0deg) !important; }
    .online-coin.settled .online-coin-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); b
--- chat #3 offset=198368 ---
ateY(0deg) !important; }
    .online-coin.settled .online-coin-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-be
--- chat #4 offset=198388 ---
t; }
    .online-coin.settled .online-coin-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; pad
--- chat #5 offset=198435 ---
 { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: colu
--- chat #6 offset=198492 ---
hatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; co
--- chat #7 offset=198551 ---
tom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
 
--- chat #8 offset=198596 ---
);
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; pad
--- chat #9 offset=198612 ---
: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; 
--- chat #10 offset=198924 ---
 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; borde
--- chat #11 offset=199395 ---
ight: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { 
--- chat #12 offset=199456 ---
 center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap
--- chat #13 offset=199612 ---
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose bu
--- chat #14 offset=199657 ---
d; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-
--- chat #15 offset=199725 ---
(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18
--- chat #16 offset=199867 ---
olid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgb
--- chat #17 offset=199966 ---
6); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rg
--- chat #18 offset=200117 ---
isplay: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transf
--- chat #19 offset=200240 ---
 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
 
--- chat #20 offset=200369 ---
tton { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
  
--- chat #21 offset=200458 ---
 overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
   
--- chat #22 offset=200626 ---
13px; text-align: center; }
    .online-chat-message { max-width: 84%; padding: 7px 10px; border-radius: 12px; line-height: 1.4; overflow-wrap: anywhere; white-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacS
--- chat #23 offset=200784 ---
e-space: pre-wrap; }
    .online-chat-message.mine { align-self: flex-end; background: #245ac7; color: #fff; border-bottom-right-radius: 4px; }
    .online-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-s
--- chat #24 offset=200939 ---
-chat-message.remote { align-self: flex-start; background: #253044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: nor
--- chat #25 offset=201001 ---
3044; color: #f4f7ff; border-bottom-left-radius: 4px; }
    .online-chat-meta { display: block; margin-top: 3px; font-size: 9px; opacity: .65; }
    .online-chat-emotes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      padding: 8px 9px 0;
      border-top: 1px solid rgba(94,119,166,.25);
    }
    .online-chat-emotes button {
      min-width: 42px;
      padding: 6px 8px;
      border-radius: 10px;
      font-size: 17px;
      line-height: 1;
    }
    .online-chat-compose { display: grid; grid-template-columns: minmax(0,1fr) auto; gap: 7px; padding: 9px; border-top: 1px solid rgba(94,119,166,.35); }
    .online-chat-compose input { min-width: 0; width: 100%; }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
--- chat #26 offset=201686 ---
 }
    .online-chat-compose button { min-width: 62px; }
    .online-resync-toast {
      position: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
      text-rendering: auto !important;
    }
    h1,h2,h3,h4,p,b,strong,span,div,label,button,input,select,textarea {
      word-spacing: normal !important;
    }
    .developer-seed-control { display:none !important; }
  </style>
<style id="bossRedLiveGlowHardFix">
  #onlineChatBtn[hidden] { display:none !important; visibility:hidden !important; pointer-events:none !important; }

  .battle-unit-card[data-boss-red-timeora="true"],
  .battle-unit-card.boss-red-live-card {
    overflow:visible !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage {
    position:relative !important;
   
--- chat #27 offset=201784 ---
 fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
      text-rendering: auto !important;
    }
    h1,h2,h3,h4,p,b,strong,span,div,label,button,input,select,textarea {
      word-spacing: normal !important;
    }
    .developer-seed-control { display:none !important; }
  </style>
<style id="bossRedLiveGlowHardFix">
  #onlineChatBtn[hidden] { display:none !important; visibility:hidden !important; pointer-events:none !important; }

  .battle-unit-card[data-boss-red-timeora="true"],
  .battle-unit-card.boss-red-live-card {
    overflow:visible !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage {
    position:relative !important;
    overflow:visible !important;
    isolation:isolate !important;
  }
  .battle-unit-card[data-boss-
--- chat #28 offset=202682 ---
font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
      text-rendering: auto !important;
    }
    h1,h2,h3,h4,p,b,strong,span,div,label,button,input,select,textarea {
      word-spacing: normal !important;
    }
    .developer-seed-control { display:none !important; }
  </style>
<style id="bossRedLiveGlowHardFix">
  #onlineChatBtn[hidden] { display:none !important; visibility:hidden !important; pointer-events:none !important; }

  .battle-unit-card[data-boss-red-timeora="true"],
  .battle-unit-card.boss-red-live-card {
    overflow:visible !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage {
    position:relative !important;
    overflow:visible !important;
    isolation:isolate !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage::before,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage::before {
    content:"";
    position:absolute;
    z-index:0;
    left:50%;
    top:52%;
    width:clamp(48px, 34%, 66px);
    height:clamp(48px, 34%, 66px);
    transform:translate(-50%,-50%) scale(.98);
    border-radius:50%;
    pointer-events:none;
    background:radial-gradient(circle,
      rgba(255,92,108,.32) 0 10%,
      rgba(255,33,62,.18) 27%,
      rgba(190,0,37,.07) 48%,
      transparent 72%);
    filter:blur(4px);
    box-shadow:
      0 0 7px rgba(255,52,78,.24),
      0 0 14px rgba(210,0,42,.11);
    opacity:.46;
    animation:bossLiveRedGlowPulse 1.8s ease-in-out infinite alternate;
  }
  .battle-unit-card.bench-card[data-boss-red-timeora="true"] .boss-red-live-stage::before,
  .battle-unit-card.bench-card.boss-red-live-card .boss-red-live-stage::before {
  
--- chat #29 offset=1749072 ---

--- chat #30 offset=2512290 ---

===== onlineChat count=140 =====
--- onlineChat #1 offset=197785 ---
px #000, 0 0 5px #000 !important;
      box-shadow: inset 0 0 0 7px rgba(255,255,255,.16), inset 0 0 28px rgba(0,0,0,.8) !important;
    }
    .online-coin.settled {
      animation: none !important;
      transform: translateY(0) rotateY(var(--coin-end)) rotateX(0) scale(1) !important;
      transform-style: preserve-3d !important;
    }
    .online-coin.settled .online-coin-face {
      display: flex !important;
      backface-visibility: hidden !important;
      -webkit-backface-visibility: hidden !important;
    }
    .online-coin.settled .online-coin-gold { transform: rotateY(0deg) !important; }
    .online-coin.settled .online-coin-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); b
--- onlineChat #2 offset=198382 ---
portant; }
    .online-coin.settled .online-coin-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; pad
--- onlineChat #3 offset=198429 ---
n-mono { transform: rotateY(180deg) !important; }

    #onlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: colu
--- onlineChat #4 offset=198486 ---
nlineChatBtn {
      position: fixed;
      right: 5px;
      bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; co
--- onlineChat #5 offset=198545 ---
   bottom: calc(126px + env(safe-area-inset-bottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
 
--- onlineChat #6 offset=198590 ---
ottom));
      z-index: 236;
      width: 38px;
      height: 38px;
      min-width: 38px;
      padding: 0;
      border-radius: 999px;
      display: grid;
      place-items: center;
      background: linear-gradient(180deg, rgba(42,63,104,.98), rgba(27,39,68,.98));
      border: 1px solid rgba(112,145,208,.9);
      box-shadow: 0 10px 24px rgba(0,0,0,.42), inset 0 1px 0 rgba(255,255,255,.12);
      font-size: 18px;
      line-height: 1;
    }
    body:not(.online-battle-chat-active) #onlineChatBtn { display:none!important; }
    #onlineChatBtn[hidden] { display: none !important; }
    #onlineChatPanel[hidden] { display: none !important; }
    #onlineChatBtn::before { content: "💬"; }
    #onlineChatBtn .online-chat-unread {
      position: absolute; right: -5px; top: -6px; min-width: 18px; height: 18px; padding: 0 5px;
      display: grid; place-items: center; border-radius: 999px; background: #ff466d; color: #fff;
      font-size: 10px; font-weight: 1000; box-shadow: 0 0 10px rgba(255,70,109,.72);
    }
    .online-chat-panel {
      position: fixed; z-index: 24000; right: 12px; bottom: 12px; width: min(390px, calc(100vw - 24px));
      max-height: min(540px, 72vh); display: grid; grid-template-rows: auto minmax(130px, 1fr) auto auto;
      border: 1px solid rgba(111,147,211,.7); border-radius: 16px; overflow: hidden;
      background: rgba(9,14,25,.96); box-shadow: 0 18px 55px rgba(0,0,0,.58), 0 0 22px rgba(63,119,226,.18);
      backdrop-filter: blur(12px);
    }
    .online-chat-panel[hidden] { display: none !important; }
    .online-chat-head { display: flex; align-items: center; justify-content: space-between; gap: 8px; padding: 10px 12px; background: rgba(29,43,70,.92); }
    .online-chat-head b { font-size: 15px; }
    .online-chat-head button { min-width: 42px; padding: 6px 9px; }
    .online-chat-messages { overflow-y: auto; overscroll-behavior: contain; padding: 10px; display: flex; flex-direction: column; gap: 7px; }
    .online-chat-empty { margin: auto; color: var(--muted); font-size: 13px; text-align: center; }
    .online-chat-message { max-width: 84%; pad
--- onlineChat #7 offset=201778 ---
ition: fixed; z-index: 25000; left: 50%; bottom: 18px; transform: translateX(-50%);
      padding: 8px 13px; border: 1px solid rgba(119,168,255,.72); border-radius: 999px;
      background: rgba(13,24,43,.94); color: #dceaff; font-weight: 800; font-size: 12px;
      box-shadow: 0 8px 24px rgba(0,0,0,.4); pointer-events: none;
      animation: onlineResyncToast 1.8s ease forwards;
    }
    @keyframes onlineResyncToast { 0%{opacity:0;transform:translate(-50%,10px)} 18%,72%{opacity:1;transform:translate(-50%,0)} 100%{opacity:0;transform:translate(-50%,-8px)} }
    @media(max-width:600px){
      .online-chat-panel { right: 6px; bottom: 6px; width: calc(100vw - 12px); max-height: 66vh; }
      #onlineChatBtn { right: 5px; bottom: calc(126px + env(safe-area-inset-bottom)); }
    }
  </style>
<style id="stableKoreanTextMetricsPatch">
    html, body, button, input, select, textarea, option {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
      text-rendering: auto !important;
    }
    h1,h2,h3,h4,p,b,strong,span,div,label,button,input,select,textarea {
      word-spacing: normal !important;
    }
    .developer-seed-control { display:none !important; }
  </style>
<style id="bossRedLiveGlowHardFix">
  #onlineChatBtn[hidden] { display:none !important; visibility:hidden !important; pointer-events:none !important; }

  .battle-unit-card[data-boss-red-timeora="true"],
  .battle-unit-card.boss-red-live-card {
    overflow:visible !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage {
    position:relative !important;
    overflow:visible !important;
    isolation:isolate !important;
  }
  .battle-unit-card[data-boss-
--- onlineChat #8 offset=202676 ---
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "SamsungOneKorean", "Noto Sans KR", "Noto Sans CJK KR", "Apple SD Gothic Neo", "Malgun Gothic", sans-serif !important;
      -webkit-text-size-adjust: 100%;
      text-size-adjust: 100%;
      font-kerning: auto !important;
      font-variant-ligatures: normal !important;
      font-synthesis: weight style !important;
      text-rendering: auto !important;
    }
    h1,h2,h3,h4,p,b,strong,span,div,label,button,input,select,textarea {
      word-spacing: normal !important;
    }
    .developer-seed-control { display:none !important; }
  </style>
<style id="bossRedLiveGlowHardFix">
  #onlineChatBtn[hidden] { display:none !important; visibility:hidden !important; pointer-events:none !important; }

  .battle-unit-card[data-boss-red-timeora="true"],
  .battle-unit-card.boss-red-live-card {
    overflow:visible !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage {
    position:relative !important;
    overflow:visible !important;
    isolation:isolate !important;
  }
  .battle-unit-card[data-boss-red-timeora="true"] .boss-red-live-stage::before,
  .battle-unit-card.boss-red-live-card .boss-red-live-stage::before {
    content:"";
    position:absolute;
    z-index:0;
    left:50%;
    top:52%;
    width:clamp(48px, 34%, 66px);
    height:clamp(48px, 34%, 66px);
    transform:translate(-50%,-50%) scale(.98);
    border-radius:50%;
    pointer-events:none;
    background:radial-gradient(circle,
      rgba(255,92,108,.32) 0 10%,
      rgba(255,33,62,.18) 27%,
      rgba(190,0,37,.07) 48%,
      transparent 72%);
    filter:blur(4px);
    box-shadow:
      0 0 7px rgba(255,52,78,.24),
      0 0 14px rgba(210,0,42,.11);
    opacity:.46;
    animation:bossLiveRedGlowPulse 1.8s ease-in-out infinite alternate;
  }
  .battle-unit-card.bench-card[data-boss-red-timeora="true"] .boss-red-live-stage::before,
  .battle-unit-card.bench-card.boss-red-live-card .boss-red-live-stage::before {
  
--- onlineChat #9 offset=6168826 ---
("#modal").classList.remove("open");
        $("#pvpResultMenu").onclick = () => window.OnlineRuntime?.returnToSetup?.("pvp-result-menu");
      }
      const NavigationApplication = (() => {
        const project = (id) => {
          hideCardTooltip();
          $$(".screen").forEach((x) => x.classList.toggle("active", x.id === id));
          if (id !== "game") stopBgm();
          if (id === "setup") {
            document.querySelectorAll(".battle-emote").forEach((node) => node.remove());
            (document.scrollingElement || document.documentElement)?.scrollTo({ top: 0, left: 0, behavior: "auto" });
            syncGrandFieldBackground(null);
          }
          window.__megaSyncOnlineChatUI?.();
          return true;
        };
        const request = (id, reason = "navigation") => {
          if (id === "setup" && game) game.paused = true;
          const committed = project(id);
          return Object.freeze({ committed, id, reason, paused:!!game?.paused });
        };
        const resumeBattle = (reason = "battle-resume", options = {}) => {
          if (game && !game.winner) game.paused = false;
          if (options.render === true && game) render();
          return Object.freeze({ committed:true, reason, paused:!!game?.paused });
        };
        return Object.freeze({ request, resumeBattle });
      })();
      function openChoice(html, items) {
        $("#modalContent").innerHTML = html + '<div class="choice-list"></div>';
        let list = $("#modalContent .choice-list");
        items.forEach((x) => {
          let b = document.createElement("button");
          b.textContent = x.label;
          b.onclick = () => {
            $("#modal").classList.remove("open");
            x.fn();
          };
          list.appendChild(b);
        });
        $("#modal").classList.add("open");
      }
      function showCodex() {
        let html =
          `<h2>카드 도감 — 총 ${CARDS.length}장</h2><p>카드 설명은 컴퓨터에서 마우스를 올리거나 모바일에서 반초간 길게 눌러 확인합니다. 브케노↔휴브, 이루마↔각성이루마, 화락나리↔루나리↔호리진탈은 하나의 폼 패밀리이며, 리그 편성에서는 해당 구성에서 사용 가능한 폼만 표시·전환됩니다.</p><div class="cards-grid">` +

--- onlineChat #10 offset=6179392 ---
tion);
        irumaForm = selected.includes("awakenedIruma")
          ? "awakenedIruma"
          : "iruma";
        bkenoForm = selected.includes("huve") ? "huve" : "bkeno";
        horizonForm = selected.includes("horizintal") ? "horizintal" : selected.includes("lunari") ? "lunari" : "flameflower";
        plusStarSelection = resolvePlusStarChoice(plusStarSelection, selected, "P");
        renderSetup(false);
      };
      $("#startBtn").onclick = () => startBattle();
      $("#menuCodexBtn").onclick = showCodex;
      $("#closeModal").onclick = () => {
        hideCardTooltip();
        if (window.OnlineRuntime?.ui?.closeOwnedModal?.("global-modal-close")) {
          window.__megaSyncOnlineChatUI?.();
          return;
        }
        $("#modal").classList.remove("open");
        window.__megaSyncOnlineChatUI?.();
        if (game && !game.winner) {
          render();
          if (!processPendingTraitChoice()) scheduleAI();
        }
      };
      function setBattleLogOpen(open) {
        const logBox = $("#log"), mobile = window.matchMedia("(max-width: 900px)").matches;
        logBox.classList.toggle("hidden", !open);
        $("#logPanel")?.classList.toggle("log-open", !!open);
        $("#collapseLog").textContent = open ? (mobile ? "닫기" : "접기") : (mobile ? "로그" : "펼치기");
      }
      $("#collapseLog").onclick = () => setBattleLogOpen($("#log").classList.contains("hidden"));
      if (window.matchMedia("(max-width: 900px)").matches) setBattleLogOpen(false);
      $("#simBtn").onclick = runSimulation;
      $("#leagueMode").onchange = (e) => setLeague(e.target.value);
      $$(".leagueTab").forEach(
        (x) => (x.onclick = () => setLeague(x.dataset.league)),
      );
      $$(".composition-choice").forEach(
        (x) => (x.onclick = () => setComprehensiveComposition(x.dataset.composition)),
      );
      $("#aiModeBtn").onclick = () => {
        $("#mode").value = "aivai";
        $("#simResult").scrollIntoView?.({ behavior: "smooth" });
      };
      $$('[data-nav]').forEach((x) => (x.onclick = () => NavigationApplication.request(x.dataset.nav)));
 
--- onlineChat #11 offset=6179509 ---
  bkenoForm = selected.includes("huve") ? "huve" : "bkeno";
        horizonForm = selected.includes("horizintal") ? "horizintal" : selected.includes("lunari") ? "lunari" : "flameflower";
        plusStarSelection = resolvePlusStarChoice(plusStarSelection, selected, "P");
        renderSetup(false);
      };
      $("#startBtn").onclick = () => startBattle();
      $("#menuCodexBtn").onclick = showCodex;
      $("#closeModal").onclick = () => {
        hideCardTooltip();
        if (window.OnlineRuntime?.ui?.closeOwnedModal?.("global-modal-close")) {
          window.__megaSyncOnlineChatUI?.();
          return;
        }
        $("#modal").classList.remove("open");
        window.__megaSyncOnlineChatUI?.();
        if (game && !game.winner) {
          render();
          if (!processPendingTraitChoice()) scheduleAI();
        }
      };
      function setBattleLogOpen(open) {
        const logBox = $("#log"), mobile = window.matchMedia("(max-width: 900px)").matches;
        logBox.classList.toggle("hidden", !open);
        $("#logPanel")?.classList.toggle("log-open", !!open);
        $("#collapseLog").textContent = open ? (mobile ? "닫기" : "접기") : (mobile ? "로그" : "펼치기");
      }
      $("#collapseLog").onclick = () => setBattleLogOpen($("#log").classList.contains("hidden"));
      if (window.matchMedia("(max-width: 900px)").matches) setBattleLogOpen(false);
      $("#simBtn").onclick = runSimulation;
      $("#leagueMode").onchange = (e) => setLeague(e.target.value);
      $$(".leagueTab").forEach(
        (x) => (x.onclick = () => setLeague(x.dataset.league)),
      );
      $$(".composition-choice").forEach(
        (x) => (x.onclick = () => setComprehensiveComposition(x.dataset.composition)),
      );
      $("#aiModeBtn").onclick = () => {
        $("#mode").value = "aivai";
        $("#simResult").scrollIntoView?.({ behavior: "smooth" });
      };
      $$('[data-nav]').forEach((x) => (x.onclick = () => NavigationApplication.request(x.dataset.nav)));
      installCardTooltipInteractions();
      setLeague("mega");
      /* Invite-link auto join is owned once by canon
--- onlineChat #12 offset=6507402 ---
in");
          if (!coin || event.animationName !== "onlineCoinFlip") return;
          settleOnlineCoin(coin);
        }, true);
        const onlineCoinObserver = new MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && D
--- onlineChat #13 offset=6507445 ---
nName !== "onlineCoinFlip") return;
          settleOnlineCoin(coin);
        }, true);
        const onlineCoinObserver = new MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentA
--- onlineChat #14 offset=6507484 ---
       settleOnlineCoin(coin);
        }, true);
        const onlineCoinObserver = new MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Rel
--- onlineChat #15 offset=6507526 ---
true);
        const onlineCoinObserver = new MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn mu
--- onlineChat #16 offset=6507569 ---
ew MutationObserver(records => {
          for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn must stay paired with packet.revision.
      
--- onlineChat #17 offset=6507603 ---
         for (const record of records) for (const node of record.addedNodes) {
            if (!(node instanceof Element)) continue;
            const coin = node.matches?.(".online-coin") ? node : node.querySelector?.(".online-coin");
            if (coin) window.TimerManager.setTimeout(() => settleOnlineCoin(coin), 1520);
          }
        });
        onlineCoinObserver.observe(document.documentElement, {childList:true, subtree:true});

        let onlineStateRetryTimer = null;
        let onlineChatRetryTimer = null;
        const onlineChatMessages = [];
        const onlineChatSeen = new Set();
        const onlineChatPending = new Map();
        let onlineChatUnread = 0;
        let onlineChatPanelOpen = false;

        function onlinePacketMatchId(packet) {
          return String(packet?.matchId || onlineCurrentMatchId?.() || p2p?.matchId || game?.matchId || "online");
        }
        function onlineConnectionReady() {
          const open=!!p2p?.conn?.open;
          if(open&&p2p&&!p2p.connected)p2p.connected=true;
          return open;
        }
        function sendOnlinePacketSafe(packet) {
          try {
            if (!onlineConnectionReady()) return false;
            return p2p.conn.send(packet) !== false;
          } catch (error) {
            if (window.DEBUG_BATTLE_EVENTS) console.warn("[online-resume] send failed", packet?.type, error);
            return false;
          }
        }
        // 복귀 동기화는 백그라운드에서 조용히 처리한다. 입력 위에 상태 문구를 띄우지 않는다.

        // 기존 권위 상태 전송을 보존하면서 마지막 패킷을 ACK가 올 때까지 재전송한다.

        function retryReliableOnlineState(force = false) {
          if (!onlineConnectionReady() || p2p?.role !== "host" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn must stay paired with packet.revision.
          p2p.__reliableStateSentAt = Da
--- onlineChat #18 offset=6509273 ---
st" || game?.mode !== "online") return;
          const packet = p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn must stay paired with packet.revision.
          p2p.__reliableStateSentAt = Date.now();
          sendOnlinePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="on
--- onlineChat #19 offset=6509338 ---
p2p.__reliableStatePacket;
          if (!packet) return;
          if (Number(p2p.__reliableStateAck || 0) >= Number(packet.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn must stay paired with packet.revision.
          p2p.__reliableStateSentAt = Date.now();
          sendOnlinePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatIn
--- onlineChat #20 offset=6509460 ---
et.revision || 0)) return;
          if (!force && Date.now() - Number(p2p.__reliableStateSentAt || 0) < 720) return;
          // Reliable retries are immutable: state/turn must stay paired with packet.revision.
          p2p.__reliableStateSentAt = Date.now();
          sendOnlinePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
  
--- onlineChat #21 offset=6509741 ---
nePacketSafe(packet);
        }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click"
--- onlineChat #22 offset=6509764 ---
       }

        function chatTimeLabel(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote
--- onlineChat #23 offset=6509799 ---
Label(ts) {
          try { return new Date(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textC
--- onlineChat #24 offset=6509839 ---
ate(ts).toLocaleTimeString([], {hour:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
       
--- onlineChat #25 offset=6509873 ---
ur:"2-digit", minute:"2-digit"}); }
          catch { return ""; }
        }
        function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineC
--- onlineChat #26 offset=6509956 ---
  function ensureOnlineChatUI() {
          let button = document.getElementById("onlineChatBtn");
          if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault(
--- onlineChat #27 offset=6510062 ---
   if (!button) {
            button = document.createElement("button");
            button.id = "onlineChatBtn";
            button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault();
              const input = panel.querySelector("#onlineChatInput");
              const value = String
--- onlineChat #28 offset=6510184 ---
    button.type = "button";
            button.hidden = true;
            button.setAttribute("aria-label", "온라인 채팅 열기");
            button.title = "온라인 채팅";
            document.body.appendChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault();
              const input = panel.querySelector("#onlineChatInput");
              const value = String(input.value || "").trim();
              if (!value) return;
              input.value = "";
              sendOnlineChat
--- onlineChat #29 offset=6510372 ---
endChild(button);
            button.onclick = () => {
              onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault();
              const input = panel.querySelector("#onlineChatInput");
              const value = String(input.value || "").trim();
              if (!value) return;
              input.value = "";
              sendOnlineChat(value);
            });
          }
          return {button, panel};
        }
        function renderOnlineChatMessages() {
          const box = document.getElementById("onlineChatMess
--- onlineChat #30 offset=6510429 ---
            onlineChatPanelOpen = !onlineChatPanelOpen;
              onlineChatUnread = 0;
              syncOnlineChatUI();
              if (onlineChatPanelOpen) window.TimerManager.setTimeout(() => document.getElementById("onlineChatInput")?.focus(), 0);
            };
          }
          let panel = document.getElementById("onlineChatPanel");
          if (!panel) {
            panel = document.createElement("section");
            panel.id = "onlineChatPanel";
            panel.className = "online-chat-panel";
            panel.hidden = true;
            panel.innerHTML = `<div class="online-chat-head"><b>온라인 채팅</b><button id="onlineChatClose" type="button">닫기</button></div><div id="onlineChatMessages" class="online-chat-messages"></div><div class="online-chat-emotes" id="onlineChatEmotes">${["👍","🔥","😮","😂","💢","GG"].map(e => `<button type="button" data-chat-emote="${e}" aria-label="${e}">${e}</button>`).join("")}</div><form id="onlineChatForm" class="online-chat-compose"><input id="onlineChatInput" maxlength="120" autocomplete="off" placeholder="메시지 입력"><button type="submit" class="primary">전송</button></form>`;
            document.body.appendChild(panel);
            panel.querySelector("#onlineChatClose").onclick = () => { onlineChatPanelOpen = false; syncOnlineChatUI(); };
            panel.querySelectorAll("[data-chat-emote]").forEach(btn => {
              btn.addEventListener("click", () => sendOnlineEmote(btn.dataset.chatEmote || btn.textContent || "👍"));
            });
            panel.querySelector("#onlineChatForm").addEventListener("submit", event => {
              event.preventDefault();
              const input = panel.querySelector("#onlineChatInput");
              const value = String(input.value || "").trim();
              if (!value) return;
              input.value = "";
              sendOnlineChat(value);
            });
          }
          return {button, panel};
        }
        function renderOnlineChatMessages() {
          const box = document.getElementById("onlineChatMessages");
          if (!box) return;
          box.textCon
===== online-room-chat count=0 =====
===== data-emote count=2 =====
--- data-emote #1 offset=6359639 ---
king" ? "자동 매칭 대기실" : `온라인 대전 룸 · ${p2p.roomCode}`;
          const currentInvader = currentSelectedInvader(), portalAllowed = LeagueFormatOwner.invaderAllowed(league, composition);
          root.innerHTML = `<h2>${title}</h2><p class="tiny muted">${LeagueFormatOwner.definition(league).label} · ${LeagueFormatOwner.rulesText(league, composition)}</p><p id="roomReadyState">${onlineRoomReadyText()}</p><div id="onlineRoomDeck" class="online-room-grid"></div>${portalAllowed ? InvaderPortalView.buttonHTML({buttonId:"onlineInvaderPortalButton", selectedId:currentInvader, areaClass:"online-invader-portal-area"}) : ""}<div class="emote-bar"><b>감정 표현</b>${["👍","🔥","😮","😂","💢","GG"].map(e => `<button data-emote="${e}">${e}</button>`).join("")}</div><div class="choice-list"><button id="onlineRoomStart" class="primary">${p2p.role === "host" ? "전투 시작" : "상대 준비 대기"}</button><button id="onlineRoomLeave">연결 끊기</button></div><p class="tiny muted">시작 화면과 동일한 리그 구성 규칙을 사용합니다. 폼 캐릭터는 이 리그에서 합법인 폼만 전환됩니다.${portalAllowed ? " 침략자는 초차원 포탈에서 최대 한 명 선택할 수 있습니다." : ""}</p>`;
          const box = document.getElementById("onlineRoomDeck");
          const draw = () => {
            box.replaceChildren();
            const normalPool = LeagueFormatOwner.setupPool(league, composition, selected, plusStarSelection, onlineLocalTeam());
            const seen = new Set();
            for (const rawId of normalPool) {
              const id = rawId;
              if (seen.has(id)) continue;
              seen.add(id);
              const c = CARDS.find(card => card.id === id);
              if (!c || isInvaderCardId(id)) continue;
              const element = document.createElement("div");
              element.tabIndex = 0;
              element.setAttribute("role", "button");
              const slotBlocked = !selected.includes(id) && !LeagueFormatOwner.partialValid([...selected,id], league, composition, plusStarSelection, onlineLocalTeam());
              element.className = `card compact-card selectable online-room-card ${selected.includes(id) ? "selected" : ""} ${slotBlocked ? "league-slot-blocked" : "
--- data-emote #2 offset=6366122 ---
:button.dataset.plusstarCard,capacity:4,
                validate:(proposal,choice)=>LeagueFormatOwner.partialValid(proposal, onlineLeagueNow(), onlineCompositionNow(), choice, onlineLocalTeam()),
                commit:(nextDeck,nextChoice)=>DeckDraftApplication.commit(nextDeck,nextChoice),
                feedback:message=>{const state=document.getElementById("roomReadyState");if(state)state.textContent=message;},
              });
              if(result.ok){submitDeck();draw();}
            });
            PlusStarBannerPresentationOwner.sync(box,onlinePlusContext,selected,plusStarSelection);
            projectControls();
          };
          draw();
          root.querySelectorAll("[data-emote]").forEach(button => button.onclick = () => sendOnlineEmote(button.dataset.emote));
          const portal = document.getElementById("onlineInvaderPortalButton");
          if (portal) portal.onclick = openOnlineInvaderPortal;
          document.getElementById("onlineRoomLeave").onclick = () => {
            NetworkEngine.closeOnline({ reason: "online-room-leave", clearChat: true, returnToLobby: true });
            closeRoom({ reason: "online-room-leave" });
          };
          document.getElementById("onlineRoomStart").onclick = startOnlineRoomBattleOnce;
          projectControls();
          modal.classList.add("open", "online-room-r22");
          if (changed) submitDeck("sanitize-after-room-render");
          restoreRoomScroll(snapshot);
          syncChat();
          renderCount++;
          return true;
        };
        const acquireSurface = (options = {}) => {
          const identity=runtimeIdentity();

          const rendered=renderRoomSurface(options),surface=inspectSurface();
          const ok=!!(rendered&&surface.connected&&surface.visible&&surface.interactionReady&&(surface.surfaceKind==="PORTAL"||(surface.cardCount>0&&surface.enabledCardCount>0)));
          const result=Object.freeze({ok,rendered,surface,reason:ok?null:"LOBBY_SURFACE_ACQUISITION_FAILED"});
          record("surface-acquire-result",{reason:String(options.reason||"projection"),ok,targe
===== data-emoji count=0 =====