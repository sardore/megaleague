import {test,expect} from '@playwright/test';import {withClients} from './gate-harness.js';import {pairByRoomCode,selectFour,waitRemoteCount,startBattle,runtimeSummary,touchFirstLegalAction} from '../helpers/player-path.js';
const count=Number(process.env.CP32_START_CASES||20);for(let i=1;i<=count;i++)test(`initiative start ${i}/${count}`,()=>withClients(`start-${i}`,async({host,guest})=>{await pairByRoomCode(host.page,guest.page);await selectFour(host.page);await waitRemoteCount(guest.page,4);await selectFour(guest.page);await waitRemoteCount(host.page,4);await startBattle(host.page,guest.page);const h=await runtimeSummary(host.page),g=await runtimeSummary(guest.page);expect(h.runtime?.matchId||h.diagnostic?.matchIdHash).toBeTruthy();const p=h.actions?host.page:guest.page;await touchFirstLegalAction(p);return {h,g};}));
