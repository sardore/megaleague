import {test,expect} from '@playwright/test';import {withClients} from './gate-harness.js';import {actualPlayerPath,runtimeSummary,touchFirstLegalAction} from '../helpers/player-path.js';
const count=Number(process.env.CP32_BATTLE_BACKGROUND_CASES||20);for(let i=1;i<=count;i++)test(`battle web background resume ${i}/${count}`,()=>withClients(`battle-bg-${i}`,async({host,guest})=>{await actualPlayerPath(host.page,guest.page);const bg=i%2?host:guest;const blank=await bg.context.newPage();await blank.goto('about:blank');await blank.bringToFront();if(i%3===0)await bg.context.setOffline(true);await new Promise(r=>setTimeout(r,i%4===0?30000:5000));if(i%3===0)await bg.context.setOffline(false);await bg.page.bringToFront();await expect.poll(async()=>(await runtimeSummary(bg.page)).actions,{timeout:45000}).toBeGreaterThan(0);await touchFirstLegalAction(bg.page);return await runtimeSummary(bg.page);}));
